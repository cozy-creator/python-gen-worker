# th#1562 / pgw#944 — sha256 of worker_scheduler.proto, the worker<->orchestrator
# wire contract. tensorhub's copy is canonical; python-gen-worker vendors it.
# BOTH repos commit this same digest, so a one-sided edit fails scripts/proto-drift-check.sh
# offline, in the repo where it happened. Regenerate with:
#   sha256sum internal/orchestrator/grpc/proto/worker_scheduler.proto
5f7cec2795c92fe9b5cee41114a75ca6e6f46deac0c171d91454272ba59ab51c

# th#2082: `RunJob.output_mode` -> `media_bytes`, enum `OutputMode` ->
# `MediaBytes` (`MEDIA_BYTES_UNSPECIFIED|URL|INLINE`). NAME ONLY — field 9 and
# the enum numbers 0/1/2 are unchanged, so the wire is byte-identical and a hub
# on an older build is unaffected. tensorhub lands the same hunk in lockstep;
# the two copies still differ by this repo's th#1941 composed-manifest hunks,
# so the two digests remain legitimately different (tensorhub's PROTO_DIGEST
# says the same thing from its side).

# pgw#1336 / th#2052: `DesiredIntentKind.DESIRED_INTENT_KIND_RUN_JOB = 6` plus
# `RunJob.intent_kind|intent_id|goal_id|phase_budget_s` (fields 18-21), applied
# to this vendored copy byte-identically to tensorhub's canonical hunk at
# `2c6f75271`. All ADDITIVE — nothing existing moves, so a hub on an older
# build is unaffected. The two copies still differ only by th#1941's
# composed-manifest hunks, as above.
#
# Do NOT "fix" the remaining divergence with scripts/vendor-proto.sh: a
# whole-file resync would DELETE the th#1941 hunks this tree depends on. Apply
# a peer's hunk surgically and re-record the digest, which is what this did.

# th#2148: `RunJob.file_base_url = 22` — the hub's own base URL on the JOB
# dispatch, beside the capability token it is half of, applied surgically
# (NOT via vendor-proto.sh, which would delete th#1941's hunks) and byte-
# identically to tensorhub's canonical hunk. ADDITIVE. This is the credential's
# ONE home for a job: the compute child that runs job N+1 on a held-open pod is
# a process the hub never HelloAck'd, so pod-lifetime state cannot carry it.

# pgw#1415: th#2140's `ModelBinding.model = 9` and `LoraOverlay.model = 4`
# applied surgically (NOT vendor-proto.sh, which would delete th#1941's hunks),
# byte-identically to tensorhub's canonical hunk at `b7ac3ac49`. This is a
# LATE vendor and it cost a fleet-wide defect: tensorhub shipped `model = 9`
# on 2026-08-18 while this copy still called 9 `manifest_digest`, so the hub
# stamped a model NAME into a field this worker read as a fetch POINTER, and
# every v2 checkpoint's per-checkpoint defaults row was parsed and discarded.
#
# The collision breaks toward the hub: it is the only SENDER of either field
# (`scheduler_dispatch.go` stamps `mb.Model` on every dispatch; nothing has
# ever stamped `manifest_digest` — th#1941's hub leg is a PARKED DRAFT,
# tensorhub PR #1219, whose proto hunk predates th#2140 and would RE-ADD the
# collision at 9). So th#1941's `manifest_digest` moves 9 -> 10 here, and that
# leg must land its sender at 10.

# pgw#1436: `WorkerResources.capability_reason_class = 15` and
# `capability_detail = 16` — WHY a worker with a card reports no `gpu_sm`.
# ADDITIVE; nothing existing moves, and a healthy worker sends neither.
#
# Landed SURGICALLY here rather than by re-vendoring tensorhub's canonical
# copy: this file still carries th#1941's composed-manifest hunks, which have
# no tensorhub leg, so `vendor-proto.sh` would have DELETED them. The two
# digests therefore stay legitimately different — the same situation
# th#2082/th#2148 recorded, for the same reason.
#
# tensorhub's canonical copy carries the byte-identical hunk at
# 584bd5d65868166d9fc66369840931e78983b2405f5ebdff7830c9e75c851e64.

# tcg#56: `ActivityUpdate.graph_class` -> `graph_specialization` (field 20),
# applied SURGICALLY here and byte-identically to tensorhub's canonical hunk at
# 2c04cf4d4955d8f879cc35d424767031d6b564dd1eee08635ac015dd07fa936d — NOT via
# scripts/vendor-proto.sh, which would DELETE th#1941's composed-manifest hunks
# for the same reason th#2082 / th#2148 / pgw#1436 record above. The two digests
# therefore stay legitimately different.
#
# NAME ONLY. The field NUMBER is unchanged, so the binary wire is byte-identical
# and a hub or worker on an older build is unaffected; only the JSON name moves
# (`graphClass` -> `graphSpecialization`). That is what makes the two-repo
# landing window safe in either order.
#
# `activity.py` previously carried a one-line bridge (pgw's own vocabulary was
# already `graph_specialization` while the wire still said `graph_class`); this
# commit deletes it, so the two names are one again.

# th#2201 / pgw#1474: `JobResult.traceback = 9` — the traceback TAIL of the
# exception that failed a job body, formatted and bounded at this worker's own
# catch site (last frames, tail-truncated, secret-scrubbed). ADDITIVE; nothing
# existing moves, and a hub older than th#2201 simply ignores it. Applied to
# this vendored copy byte-identically to tensorhub's canonical hunk; the two
# copies still differ only by this repo's th#1941 composed-manifest hunks, so
# the two digests stay legitimately different.
#
# It exists because `safe_message` was `f"{type(exc).__name__}: {exc}"` and
# nothing else: a $0.50, 455-GPU-second flagship job failure delivered the five
# characters `'keys'` to its submitter, on a pod retired 19 seconds later.
