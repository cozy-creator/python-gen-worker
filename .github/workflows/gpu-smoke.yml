# TEMPORARY gw#421 validation workflow — removed before merge.
name: GPU Smoke (temp)

on:
  push:
    branches: [gw421-gpu-ci]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  smoke:
    runs-on: [self-hosted, gpu-4090]
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - run: nvidia-smi
      - run: uv --version && git --version && uv python list --only-installed
