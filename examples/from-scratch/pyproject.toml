[project]
name = "from-scratch"
version = "0.1.0"
dependencies = [
    "gen-worker[torch]>=0.6.2",
    "safetensors",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["from_scratch.py"]
