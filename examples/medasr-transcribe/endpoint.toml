schema_version = 1
main = "medasr_transcribe.main"

# Model bindings live in @inference_function(models={...}) in main.py. The
# per-function Resources struct on the decorator declares hardware needs.

[[build.profiles]]
name = "default"
accelerator = "cuda"
cuda = "12.8"
