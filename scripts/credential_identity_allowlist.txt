# pgw#1122 — every worker-credential read in src/gen_worker, classified.
# Enforced by scripts/lint_credential_identity.py: an unclassified read is red,
# a row matching nothing is red. Keyed <path>::<scope>::<name>, never a line.
#
# WHY THIS FILE EXISTS. The compute child holds NO worker credential by
# construction (pgw#763 delta 1): the parent strips WORKER_JWT and no frame
# carries it. A site that answers "who am I?" or "is there a hub?" by reading
# one is wrong on EVERY real serving pod, always — measured twice, one gate
# apart (pgw#1108: zero resolves; pgw#1122: every org-tier cell refused
# publisher_untrusted, the function down, three pods reaped, two replacements
# bought).
#
# There is NO IDENTITY classification. A site that needs to know which endpoint
# or org this pod serves calls gen_worker.worker_identity.viewer(), which asks
# the control PARENT when this process holds nothing and refuses TYPED when
# nobody can answer. The third instance of this bug has no label to write here.

# ---------------------------------------------------------------------------
# PARENT — runs only where a credential genuinely exists (control parent, mint
# CLI, single-process worker).
# ---------------------------------------------------------------------------
src/gen_worker/hardware_report.py::_identity_from_settings::worker_credential.current  PARENT  the child branch relays via send_boot_fatal (entrypoint.py is_compute_child); this dial is the parent's
src/gen_worker/hardware_report.py::_report_async::worker_credential.current  PARENT  same dial, same guard — the bearer for a parent-side Connect
src/gen_worker/procsplit/parent.py::ParentControl._identity::bootstrap_worker_jwt  PARENT  the parent decodes ITS credential for (worker_id, release_id) and hands both down as plain env
src/gen_worker/procsplit/parent.py::ParentControl._perform_action::current_worker_jwt  PARENT  the credential the parent attaches to every mediated child action
src/gen_worker/procsplit/parent.py::ParentControl._viewer_identity::current_worker_jwt  PARENT  pgw#1122: the parent answers identity.viewer from the token it holds
src/gen_worker/procsplit/parent.py::ParentControl._viewer_identity::bootstrap_worker_jwt  PARENT  same answer pre-rotation, before the stream has rotated one
src/gen_worker/procsplit/parent.py::<module>::env:WORKER_JWT  PARENT  _CHILD_FORBIDDEN_ENVS — the parent STRIPPING it, i.e. the reason this file exists
src/gen_worker/transport.py::Transport.current_worker_jwt::worker_credential.current  PARENT  pgw#893: the ONE accessor, reading the ONE home (the stream-local cache is deleted)
src/gen_worker/transport.py::Transport._metadata::current_worker_jwt  PARENT  the gRPC Connect's own Authorization metadata, through that accessor
src/gen_worker/transport.py::Transport._presented_credential::current_worker_jwt  PARENT  pgw#869: which credential this stream actually presented, through that accessor

# ---------------------------------------------------------------------------
# BEARER — used as an HTTP Authorization header and nothing else. Under the
# split the parent supplies the real one and IGNORES this, so "" costs nothing.
# ---------------------------------------------------------------------------
src/gen_worker/content_credentials.py::_hub_sign_claim::worker_jwt  BEARER  th#1307 c2pa.sign is parent-mediated; the parent attaches the credential
src/gen_worker/executor.py::Executor._run_job_pinned::worker_jwt_provider  BEARER  capability.renew is parent-mediated (th#639)
src/gen_worker/fleet_cells.py::CellPublisher._post::_worker_jwt  BEARER  publish-intent/complete are parent-mediated; read once so presented and blamed agree
src/gen_worker/receipts.py::_fetch_receipt_jws::worker_jwt  BEARER  compiled_graphs.receipt is parent-mediated
src/gen_worker/receipts.py::_fetch_revocations::worker_jwt  BEARER  compiled_graphs.revocations is parent-mediated

# ---------------------------------------------------------------------------
# READINESS — credential-presence as "can we reach the hub". EVERY row here
# MUST carry the pgw#1108 seam fallback (broker.active()); without it the row
# is the pgw#1108 bug wearing a label.
# ---------------------------------------------------------------------------
src/gen_worker/executor.py::Executor._boot_adopt::worker_jwt_provider  READINESS  pgw#1108: local bearer OR broker.active(), never bearer alone
src/gen_worker/fleet_cells.py::CellPublisher.enabled::_worker_jwt  READINESS  pgw#763 delta 1: base_url AND (local bearer OR broker.active())

# ---------------------------------------------------------------------------
# WIRING — the provider is handed to somebody else and never read here.
# ---------------------------------------------------------------------------
src/gen_worker/config/loader.py::<module>::env:WORKER_JWT  WIRING  the env -> Settings.bootstrap_worker_jwt mapping table
src/gen_worker/executor.py::Executor.__init__::worker_jwt_provider  WIRING  declares the provider attribute
src/gen_worker/executor.py::Executor.__init__::worker_credential.current  WIRING  its default body, until worker wiring rebinds it
src/gen_worker/executor.py::Executor._cell_publisher::worker_jwt_provider  WIRING  constructs the CellPublisher
src/gen_worker/fleet_cells.py::CellPublisher.__init__::_worker_jwt  WIRING  stores the provider
src/gen_worker/fleet_cells.py::CellPublisher.worker_jwt::_worker_jwt  WIRING  the accessor
src/gen_worker/lifecycle.py::Lifecycle.on_hello_ack::worker_jwt_provider  WIRING  arms the receipt gate and the c2pa signer with the provider; pgw#1122 moved IDENTITY off it
src/gen_worker/worker.py::Worker.__init__::current_worker_jwt  WIRING  binds the transport's credential to the executor's provider
src/gen_worker/worker.py::Worker.__init__::worker_jwt_provider  WIRING  the other half of that same binding

# ---------------------------------------------------------------------------
# RELAYED — derives a value the parent ALSO hands the child as a plain env
# value, so an empty credential here changes nothing.
# ---------------------------------------------------------------------------
src/gen_worker/lifecycle.py::Lifecycle.__init__::bootstrap_worker_jwt  RELAYED  (worker_id, release_id) claims; the child reads WORKER_ID/WORKER_RELEASE_ID relayed by parent._identity

# ---------------------------------------------------------------------------
# DIAGNOSTIC — a log/annotation that degrades to silence in the child, never a
# decision.
# ---------------------------------------------------------------------------
src/gen_worker/fleet_cells.py::CellPublisher.publish_intent::worker_jwt  DIAGNOSTIC  th#1423 401 blame ("N s past its own exp"); silent under the split, where the parent presented the token
