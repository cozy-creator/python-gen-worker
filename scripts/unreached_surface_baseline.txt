# pgw#849 guard 2 — the RATCHET.
#
# Every line is public surface in the aot/mint/serve packages that NOTHING on
# the pod's production path calls, measured on chaos 9f582ba (2026-08-01).
# `scripts/lint_unreached_surface.py` fails on any NEW line, and equally on a
# line here that has since acquired a production caller — a baseline nobody has
# to shrink is a baseline nobody reads.
#
# These are not equivalent. Roughly, by what the absence COSTS:
#
#   A gate that never runs
#     numerics_ladder.gate / compare_outputs / declared_thresholds — ✅ DELISTED
#       2026-08-02, the first entries this ratchet has ever removed. The original
#       finding is kept verbatim because it is the clearest illustration of what
#       delisting MEANS: "pgw#817's ADOPTION numerics gate. `api/decorators.py`
#       declares numerics_floor and numerics_warn per family, `activity.py`
#       declares KIND_COMPILED_GRAPH_NUMERICS, and NOTHING IN src/ IMPORTS numerics_ladder
#       AT ALL. A compiled graph about to arm is supposed to be run against the eager
#       forward it replaces and REFUSE below the floor. It never is."
#       That sentence is now FALSE, which is the whole point — pgw#848 CP12
#       (`c47dc46`) gave all three their first production callers, the guard went
#       red on STALE, and the lines came out. A symbol ceasing to be dead code is
#       this guard's nicest failure mode, and the ratchet turned the right way.
#     compile_cache.assert_closure_complete — ✅ DELISTED 2026-08-08 (pgw#1035)
#       by DELETION, with closure_completeness_gap behind it. The original
#       finding: the code-closure completeness gate ("a dynamic import is
#       hiding trace-relevant code from the recipe key"), only caller a test.
#       pgw#990 had already demoted the closure from a key axis to a memo, so
#       the honesty this gate protected stopped being load-bearing before the
#       gate was ever wired.
#     aot_serve.is_aot_artifact — ✅ DELISTED 2026-08-08 (pgw#1035) by DELETION.
#       Its own docstring said "kind sniff for the provision.enable_compiled
#       dispatch"; that dispatch never called it.
#     aot_declaration.named_dynamic_rows — ✅ DELISTED 2026-08-08 (pgw#1030) by
#       DELETION. It bridged declaration rows that NAME a Dim into per-binding
#       DynamicDim rows; nothing ever ran it.
#
#   Measured and then dropped (the pgw#848 CP1 shape)
#     boot_phases.servable_ms — "THE cold-boot number", computed, never read.
#     aot_compile_spans.dark_fraction — ✅ DELISTED 2026-08-14 (pgw#1270) by
#       EXTRACTION. The span partition is torchcg.spans now, so
#       the surface is the library's to reach, not this tree's.
#     aot_serve.realigned_inputs / served_entry_calls / EntryDispatch.refusal_counts
#       — the pgw#790/#791 adapter-fork and realignment tax, measured "rather
#       than inferred from a stderr line nobody can read", and never reported.
#       AUDITED FOR DELETION AND KEPT 2026-08-08 (pgw#1035): all three are the
#       built-but-UNWIRED class, whose fix is one caller carrying all three
#       (a JobMetrics or activity field), not three diffs that hide the gap.
#       `served_entry_calls` additionally has a real consumer — the mint rig's
#       vehicle table, driven by scripts/rig_gauntlet.py.
#     compile_cache.artifact_fx_lines / live_fx_lines — ✅ DELISTED 2026-08-08
#       (pgw#1035) by DELETION. fx forensics that fx_key_forensics does not
#       reach: the live divergence report builds its own line dicts from
#       `_fx_entry_lines`, so these two were a second way to do the same thing
#       that only the test ever took.
#
#   Dead or duplicated surface
#     compile_cache.build / prepare (the JIT build path) — ✅ DELISTED
#       2026-08-08 (pgw#1035) by DELETION, 259 lines. `build` was the
#       whole-pipeline dynamo mint superseded by aot_mint; `prepare` was a
#       swallow-everything wrapper over `seed_artifact`.
#     aot_serve.flavor_label and find_artifact — ✅ DELISTED 2026-08-08
#       (pgw#1035) by DELETION (compile_cache's same-named versions are the
#       live ones). `torch_maj_min` went with `flavor_label`, its only caller,
#       and `is_aot_ref`'s `aot-` label branch went with the label's only
#       producer.
#     aot_package.package_entry_names, guard_closure.manifest_digest.
#     aot_serve.unpack_metadata — ✅ DELISTED 2026-08-10 (pgw#1098) by
#       DELEGATION to `artifact_meta.read_metadata`. It was listed 2026-08-08
#       (pgw#1035) as "the AOT lane's own reader of its own packed envelope",
#       with the dedup deferred to the TRT-engine ratification on the reasoning
#       that waiting cost nothing. It cost $1.584 and a 92-minute sdxl mint:
#       pgw#1013 bounded the SHARED reader and not this one, so two readers of
#       one `metadata.json` disagreed about row 7's envelope — the bounded one
#       refused it, this one read it fine, and the asymmetry surfaced as a
#       LoRA-contract refusal with no root. `trt_engine.unpack_metadata` had
#       already been collapsed; this was the last of the eight.
#     compile_cache.seed_artifact — LISTED 2026-08-08 (pgw#1035). It became
#       caller-less when `prepare` was deleted, and it is deliberately NOT
#       deleted with it: it is one entry point of the delivered-inductor-cache
#       ADOPT LANE (stage -> verify in isolation -> activate under the lock).
#       RE-VERIFIED after pgw#1032 MERGED (`ddf8b8e3`), which changed the facts
#       this note was first written against: the ADOPT_COMPILE_CACHE handler is
#       now GONE, and `arm_staged_artifact` went with it. What survives is the
#       BOOT path — `enable` -> `stage_artifact` -> `artifact_drift` ->
#       `_activate_staged` — which is still reached, so the lane is NOT dead
#       code and must not be deleted as such. The open question is narrower and
#       unchanged: whether anything still PRODUCES a delivered
#       `torch-inductor-cache` artifact for that path to consume. That is the
#       ratification Paul owes. Three real-compile proofs drive the
#       transaction through THIS entry (compiled graph portability gw#611, adopt
#       collision pgw#751, executor adopt); routing them at `stage_artifact` +
#       `_activate_staged` would put tests on module privates to buy nothing.
#       It goes when the lane goes, or it gains a caller when the lane stays.
#       aot_mint.replace_spec — ✅ DELISTED 2026-08-05 by DELETION. It was a
#       one-line wrapper over `dataclasses.replace` that existed, per its own
#       docstring, "so the deferred-import dance does not have to repeat at
#       every call site". Hoisting the import removed the dance and the wrapper
#       with it: a deferred import had grown a whole public function.
#       aot_compile_pool.free_disk_bytes — ✅ DELISTED 2026-08-02 (pgw#877) by
#       DELETION, the other way a line leaves this file.
#     aot_export_parallel.groups / width_for — ✅ DELISTED 2026-08-02 by
#       WIRING (`5923622`, pgw#868 A4), then the MODULE DELETED 2026-08-08
#       (pgw#1030): the "wiring" fed a report, not a decision — export stayed
#       unconditionally serial and nothing consumed `decide()`'s six floats.
#       The 2026-08-02 note asked for the guard to be taught to see "a module
#       with NO importer at all"; this deletion is that lesson applied by
#       hand.
#
#   A DECLARED capability derived from a function this file already listed
#     models.hf_fp8_blockwise.load_hf_fp8_blockwise — ✅ DELISTED 2026-08-14
#       (pgw#1253) by WIRING: `contract_loaded_component` now detects an
#       `hf.fp8-blockwise@1` component tree and dispatches to it. Listed
#       2026-08-12 (pgw#1182) when the fence grew to the whole package.
#       pgw#1245 then hung `@implements_contract` on this same function and
#       DERIVED the image's `[decode_set]` from the marker — so the hub was
#       told this image decodes `hf.fp8-blockwise@1` on the strength of a
#       function this file had already recorded as caller-less. Nothing broke,
#       which is the point: transformers reads `quantization_config` out of
#       config.json itself, so the tree loaded through the generic arm that
#       declares `plain.bf16@1` and the contract's own verifier never ran.
#       THIS GUARD DID NOT MISS IT — the row was here the whole time. What is
#       missing is that a derivation reading the marker does not read this
#       file.
#
#   ⚠️ KNOWN BLIND SPOT — the wrapper exemption clears the WRONG function
#
#     MECHANISM: a definition is exempted when its body is a one-line call
#     into another symbol that production reaches. The reach test then runs
#     against the WRAPPEE, never the wrapper — so a wrapper with zero callers
#     of its own is cleared on the strength of the callers of the thing it
#     calls. The rule can be satisfied by a function that is not the one under
#     test, and the guard reports nothing.
#
#     MEASURED, 2026-08-02 (pgw#877): `aot_compile_pool.available_memory_bytes`
#     and `.free_device_bytes` had zero references anywhere in src/, tests/ or
#     scripts/ and were invisible to this guard for its entire life. Both are
#     now deleted. The same clearance used to cover
#     `aot_export_parallel.enabled`, whose "wrappee production already calls"
#     was `str.strip` / `dict.get` — stdlib methods, which every module reaches
#     and which therefore clear ANY one-line wrapper over them. That function
#     is gone too (pgw#1000 deleted the env gate), so the example is historical;
#     the exemption's shape is the point and it is unchanged.
#
#     KEPT, deliberately, and recorded rather than silently worked around: the
#     exemption is right far more often than it is wrong, and a rule tight
#     enough to catch these would re-list real delegating wrappers. What must
#     not happen again is treating its silence as proof of reach. Two candidate
#     tightenings, neither implemented: require the wrappee to be IN-PACKAGE
#     (kills the `str.strip` class outright), and flag any guarded module with
#     no importer in src/ regardless of per-symbol reach.
#
#   KNOWN FALSE POSITIVES
#     boot_phases.BootSpan.skipped — reached by an operator script only.
#     They stay listed rather than exempted: an exemption rule broad enough to
#     clear them would also clear real hits.
#
gen_worker.boot_phases.BootSpan.skipped()
gen_worker.boot_phases.servable_ms()
#   shape_growth.coverage_line — ✅ DELISTED 2026-08-12 (pgw#1181), by the
#   ratchet turning the RIGHT way: it acquired a production caller and the
#   landing lane did not remove its row, which left `master` itself red on
#   this guard. Removed here because every PR inherits that red; the symbol
#   is not this lane's and nothing about it changed.
#
#   THE JIT PRODUCER DIED AND THE FORMAT FOLLOWED IT — ✅ DELISTED 2026-08-12
#   (pgw#1181) by DELETION, five rows in one cut, three of them listed by
#   pgw#1178 the same day with OWNER: Phase 1 and EXPIRY: the per-entry atom.
#   Phase 1 traced the ~82 `compile_cache.pack` call sites and reported they
#   are not its tail: they exercise the JIT lane's OWN format, and format 3 is
#   one graph specialization keyed by `ek1` while `torch-inductor-cache` is a whole-compiled graph
#   FX-cache tarball with no per-specialization identity — so porting it is a category
#   error, not a task, and the format DIES. That re-assigned the three rows
#   back to this lane, and the expiry was discharged by deleting rather than
#   by waiting.
#
#   The original finding, kept because it is the clearest statement of what a
#   writer-less format looks like from inside the ratchet: pgw#1178 deleted
#   `compile_cache.mint_artifact`, the LAST producer of a
#   `torch-inductor-cache` artifact anywhere in this tree, and `pack`,
#   `artifact_metadata` and `guard_closure.closure_manifest` went unreached
#   the same instant. That answered the question the `seed_artifact` note
#   above had held open since 2026-08-08 — "whether anything still PRODUCES a
#   delivered torch-inductor-cache artifact for that path to consume" —
#   with: nothing does. The live adopt lane was consuming a format with no
#   writer, and had been for four days without anything going red.
#
#   `seed_artifact` itself is delisted here too, LISTED 2026-08-08 (pgw#1035)
#   and kept twice on the reasoning that the adopt lane it entered was still
#   reached. It was. The lane was reachable and the format was not writable,
#   which is a distinction this ratchet cannot see and a reader must.
#   `guard_closure.manifest_digest` — listed 2026-08-01, never explained —
#   digested the manifest `closure_manifest` produced, and went with it.
#

# ---------------------------------------------------------------------------
# pgw#1215 step 4 (th#1834 Phase 3) — THE ONE-SHOT MINT CHAIN LOST ITS
# PRODUCTION CALLER, AND THAT IS THE POINT OF THE CHANGE.
#
# The serving parent no longer spawns a mint child: it supervises compile
# children directly (`mint_supervisor`). `mint_delegate.py` is DELETED and its
# request vocabulary moved to `mint_process`. What is left of
# `mint_process` -> `mint_child` is reached by exactly one thing —
# `scripts/micro_mint_rig.py`, the pre-publish proof harness Paul asked for and
# which th#1834's own census marks RE-POINT, never delete.
#
# So these three are listed rather than deleted, and they are listed as a debt
# with a named end, not as an exemption:
#
#   OWNER:  pgw#1215's follow-up — re-point `micro_mint_rig.py` onto
#           `mint_supervisor`, so there is ONE mint path in the tree instead of
#           a fleet one and an operator one.
#   EXPIRY: the first 0.115.0 flag-day campaign that uses the rig. Doing it
#           BEFORE that campaign would change the harness the campaign is meant
#           to validate the reroute with, which is the wrong order.
#
# ---------------------------------------------------------------------------
# ---------------------------------------------------------------------------
# IN FLIGHT — a seam that landed AHEAD of its caller. Not 2026-08-01 debt.
#
# Segregated deliberately. An exemption list that only ever gains entries is a
# guard being retired in slow motion, so entries below carry an owning issue and
# are expected to leave, not to accumulate. If one is still here when its issue
# closes, the seam was never wired and the guard was right the first time.
#
#   pgw#868 A4 (`d6067d2`, landed 2026-08-02) — the parallel-export seam. Both
#   symbols are the DECISION rules for out-of-process row export; the only other
#   mention of `width_for` anywhere in src/ is a COMMENT in aot_mint.py:1719
#   explaining why it exists. Caught by this ratchet within hours of landing,
#   which is the first time it has flagged live work rather than history.
#
#   (pgw#903's `expected_from_plan` entry left with pgw#904: the RunAttempt
#   arming path now projects the Plan into the identity `stage_artifact`
#   verifies, exactly as the entry instructed.)
# ---------------------------------------------------------------------------
# ---------------------------------------------------------------------------
# pgw#1182 — THE SCOPE IS NOW THE WHOLE PACKAGE, and this list grew from 14 to
# 107 because of it. Nothing below was newly written; it was always there and the
# fence was pointed at one seventh of the tree.
#
# Read the count as the SIZE OF THE QUESTION, not a backlog of confirmed dead
# code. Two things were subtracted before it: nine findings that were the
# guard's own resolver bug, and nineteen whose caller is an endpoint repo
# (`scripts/author_surface_allowlist.txt`, each with its call site). What
# remains is genuinely unreached FROM THIS TREE — which is the beginning of a
# question and not the end of one. Three dispositions, and the rule is the same
# one that governed the fourteen:
#
#   WIRE IT     the caller is missing and should exist. This is the defect
#               class (`entry_workers(peak_rss_bytes=…)`, `set_guard_failure_
#               callback`) and the reason the guard exists at all.
#   DELETE IT   with its tests, never porting them (DESIGN-RULINGS §4.34).
#   PROVE IT    the caller is outside this tree — then it belongs in
#               author_surface_allowlist.txt WITH the call site, not here.
#
# A line leaves this file only one of those three ways. `--explain` shows why
# anything absent was cleared.
# ---------------------------------------------------------------------------
# ---------------------------------------------------------------------------
# pgw#1192 — TYPES are enumerated now, and the cross-repo check follows package
# RE-EXPORTS. Both were blind spots and both were found the same way: by
# checking, not by reasoning. A symbol sweep over functions alone let a dead
# type survive a whole deletion pass on a sibling lane (only the compiler caught
# it), and requiring the DEFINING module path let nine live consumers sit in
# this file marked dead — `training-endpoints` imports
# `from gen_worker.convert import Dataset` against a class defined in
# `gen_worker.convert.dataset`.
#
# Entries are `module.Symbol` for a type and `module.symbol()` for a callable.
# ---------------------------------------------------------------------------
# ---------------------------------------------------------------------------
# pgw#1271 — THE STANDING RULE, and the reason it had to become one.
#
# A row whose LEAF SYMBOL starts with `assert_` / `verify_` / `refuse_` /
# `require_` / `check_` is a GATE, and a gate on this list is a gate that
# CANNOT GO RED. It must carry an INLINE REASON or `lint_unreached_surface.py`
# fails:
#
#     gen_worker.x.assert_y()  # a test-only assertion; the property is ...
#
# "Call from tests" is an acceptable reason. Silence is not — and silence is
# what this file granted for months. The header above has named "A gate that
# never runs" as the FIRST severity class since the day it was written, and it
# has a working precedent for the right outcome (`numerics_ladder.gate`,
# delisted 2026-08-02 the moment pgw#848 gave it a caller). But findings were
# absorbed as unannotated lines, so `boot_key.assert_memo_honest` — the check
# whose own module header promises "Honesty is enforced, not trusted... A wrong
# key is never produced" — sat here with permanent tenure while the memo it was
# supposed to police produced the `graph` axis of every published `cg-key-v1`
# with nothing checking it. A gate's failure is INVISIBLE precisely because the
# reasoning around it reads like enforcement; the ratchet has to make it say so
# out loud.
#
#   pgw#1344 (this lane) — the NATIVE HF->GGUF tensor plane. `gguf_native.py`
#   reads a snapshot's tensors through tensorfs, maps and permutes them, and
#   composes the GGUF through `TensorWriter` so an unchanged tensor keeps the
#   CAS objects it already has (measured in
#   `tests/test_gguf_native_pgw1344.py`: two tensors / two objects shared with
#   the safetensors source at f32, zero at f16, and the permuted pair correctly
#   sharing nothing). It has no production caller for ONE reason, and it is a
#   reason rather than an omission: `_run_gguf_inline` cannot switch to it until
#   something produces the GGUF METADATA block — a model's vocabulary, chat
#   template and pre-tokenizer identity — and today only `convert_hf_to_gguf.py`
#   does. Shipping a guessed vocabulary is precisely the silent change pgw#1344
#   exists to prevent, so the tensor half lands proven and the switch waits.
#
#     OWNER:  pgw#1344's follow-up — a faithful native metadata/vocabulary
#             producer (or a reference run scoped to the tokenizer sidecars,
#             which is a few MB rather than the multi-GB weights). The switch
#             in `convert/convert.py::_run_gguf_inline` is one call once that
#             exists, and `convert/gguf_tools.py`'s subprocess dies with it.
#     EXPIRY: that issue. If these two rows outlive it, the tensor plane was
#             never wired and this guard was right the first time.
#
# Delisted by WIRING under pgw#1271:
#   boot_key.assert_memo_honest — called from `mint_supervisor.rule_on_boot_memo`
#     at the seal/publish seam, the one moment a pod holds a freshly TRACED
#     class set for the closure its boot may have answered from a memo.
#   parallel.cp.refuse_unless_divisible — called from
#     `install_context_parallel`, once per sharding candidate, on the declared
#     head count.
# ---------------------------------------------------------------------------

gen_worker.component_vocab.ComponentVocabulary.role_of()
gen_worker.component_vocab.declare_components()
gen_worker.component_vocab.reset_component_vocabulary()
gen_worker.config.loader.unrecognised_owned_env()
gen_worker.config.process.current()
gen_worker.families.base.family_registry()
gen_worker.geometry.FamilyGeometry.assert_declared()  # its own docstring says "Call from tests": it asserts a DECLARATION consistency property (every fit bucket is a declared compile shape) that has no runtime decision to make, and a runtime caller would be re-checking a fact the mint already refuses on.
gen_worker.input_assets.inputs_dir_for_request()
gen_worker.io.exists()
gen_worker.io.read_audio()
gen_worker.io.write_image()
gen_worker.models.attention_modes.known_attention_modes()
gen_worker.models.fp8_storage.castable_leaves()
gen_worker.models.gguf_diffusers.NormalizedTensors  # OWNER pgw#1498, EXPIRES with the ingest half. The CAS-native weight source, and the ONE line of this lane that is waiting on another issue rather than on nobody: it goes live when the ingest half normalizes a community `.gguf` into per-tensor CAS regions. `load_gguf_pipeline(source=)` takes it today and `test_gguf_wiring_pgw1498.py` asserts it builds a BIT-IDENTICAL denoiser to the `.gguf` edge source, so the swap is one constructor and the seam is measured, not hoped for.
gen_worker.models.gguf_torch.attach_lora()  # OWNER pgw#1498 (re-owned by the WIRING lane, which reached the other seven). Per-leaf adapter attachment. The production adapter route for a GGML leaf is `w8a8_lora`'s additive BRANCH — the wiring lane made `branch_modules` target punned leaves, so the whole existing adapter stack (rank buckets, staging, hot-swap, lane stamping) reaches them and needs no second mechanism. These two stay the only route for a punned leaf the branch walker cannot target (Embedding, Conv1d/3d, ConvTranspose), which no adapter in the fleet names today. "One adapter mechanism, not two" is an open simplification recorded on the pgw#1498 tracker section — it is a DELETION decision, not a wiring gap, which is why this lane baselined it instead of quietly keeping it alive with a caller.
gen_worker.models.gguf_torch.detach_lora()  # pgw#1498 — see attach_lora() above; the two are one mechanism.
gen_worker.models.hf_fp8_blockwise.dequantize_block_scaled()
gen_worker.models.native_kernels.extension_available()
gen_worker.models.native_kernels.svdq_linear_execution_lane_reason()
gen_worker.models.native_kernels.svdq_modulation_execution_lane_reason()
gen_worker.models.svdq_awq_packed.awq_packed_self_check()
gen_worker.models.svdq_fused.fused_self_check()
gen_worker.models.svdq_layout.quantize_lowrank()
gen_worker.models.tensor_layout_contract.parse_layout_id()
gen_worker.models.w4a4.quantize_tree_w4a4()
gen_worker.models.w4a4.sanitize_w4a4_state_dict()
gen_worker.procsplit.group.GroupPlan.child()
gen_worker.progress.reset()
gen_worker.progress.tracking
gen_worker.shape_growth.GrowthLedger.counts()
gen_worker.shape_growth.GrowthLedger.seen()
gen_worker.stage_timing.reconciliation()
gen_worker.url_fetch._NoRedirect.redirect_request()
gen_worker.warm_spans.WarmLedger.table()
gen_worker.worker_credential.expires_at()
gen_worker.worker_credential.reset()
gen_worker.worker_fatal.fatal_identity()

# pgw#1380's native weight source came out of this file on 2026-08-18: the
# follow-up the OWNER row named is done — `store_for` calls `native_available()`
# and constructs `NativeWeightStore`, so both are reached from src/. The third
# line, `containers_for()`, was reached by nothing anywhere and was deleted
# rather than re-baselined. All three came out together, as the row said.

gen_worker.artifact_meta.compiled_graph_metadata_fields()
gen_worker.artifact_meta.try_read_metadata()
gen_worker.boot_phases.BootCompleteness.explain()
gen_worker.boot_phases.completeness()
gen_worker.boot_phases.render_phase_table()
gen_worker.boot_stages.emit()
gen_worker.capability.HardwareUnmetError.axes()
gen_worker.capability.InsufficientDiskError.axes()
gen_worker.cli.args.build_payload()
gen_worker.cli.protocol.gen_worker_version()
gen_worker.cli.sockaddr.cleanup_listener()
gen_worker.cli.sockaddr.create_client()
gen_worker.cli.sockaddr.create_listener()
gen_worker.cli.sockaddr.display()
gen_worker.compile_posture.install()
gen_worker.cuda_root.torch_cuda_home()
gen_worker.dispatch.AdapterOrder
gen_worker.dispatch.order_from_binding()
gen_worker.env_seal.assert_seal_unchanged()  # pgw#1373: its caller was the v1 executor's per-request re-seal. `entrypoint.py` still ESTABLISHES the seal at boot (`_establish_env_seal`); nothing re-checks it mid-life yet. Wiring that into the v2 serve loop is pgw#1372's, and the assertion is kept (not deleted) because the property it guards is unchanged.
gen_worker.families.base.register_family()
gen_worker.file_hash.sha256_file()
gen_worker.graph_facts.subject_digest()
gen_worker.models.attention_modes.most_sparse_mode()
gen_worker.models.execution_lanes.ExecutionLaneUnavailableError
gen_worker.models.execution_lanes.execution_lane_body_of_binding()
gen_worker.models.execution_lanes.most_quantized_body()
gen_worker.models.execution_lanes.observed_execution_lane()
gen_worker.models.execution_lanes.parse_execution_lane_spec()
gen_worker.models.lane_residency_gate.arm_lane_residency_gate()
gen_worker.models.loading.load_component()
gen_worker.models.machine_fit.lane_candidates()
gen_worker.models.memory.cuda_allocated_bytes()
gen_worker.models.memory.rearm_offload()
gen_worker.models.memory.release_unused_pinned_host_cache()
gen_worker.models.pinned_swap.prestage_module()
gen_worker.models.provision.AppliedAttentionScope
gen_worker.models.provision.AppliedLaneScope
gen_worker.models.provision.ArmingScope
gen_worker.models.provision.load_slot()
gen_worker.models.provision.resolve_bindings()
gen_worker.models.records.record_in_use()
gen_worker.models.records.shutdown_instances()
gen_worker.models.records.vacate_record()
gen_worker.models.rung.floor_of()
gen_worker.models.rung.run_mode_of()
gen_worker.models.serve_fit.plan_serve()
gen_worker.models.serve_fit.replan()
gen_worker.models.tensor_layout_contract.UndeclaredSlotLayoutError
gen_worker.models.tensor_layout_contract.normalize_layout_demand()
gen_worker.models.tensor_layout_contract.normalize_layout_requirements()
gen_worker.models.tensor_layout_contract.normalize_layout_undeclarable()
gen_worker.models.traceability.untraceable_reason()
gen_worker.numerics_ladder.compare_outputs()
gen_worker.numerics_ladder.declared_thresholds()
gen_worker.request_context.JobContext.metric()
gen_worker.request_context._PublisherMixin.materialize_blob()
gen_worker.topology.device_group_scope
gen_worker.topology.pin_cuda_device_for_group()
gen_worker.warm_spans.WarmLedger
gen_worker.worker_fatal.report_worker_error_async()

# pgw#1475 — TWO MORE ROWS WIRED, 2026-08-19, and they are the ELEVENTH member of
# the pgw#1425 family: `_PublisherMixin.resolve_dataset()` (the reserved
# `payload.datasets` contract) and `wire_snapshots.index_snapshots()` (the
# ref-keyed snapshot view a reserved `source` materializes against) both acquired
# a production caller in `serving/reserved_repos.py`. Both were appended by the
# same `--write-baseline` sweep. The row this issue was actually filed on —
# `RequestContext._set_source_path` — was NEVER on this list: private names are
# not collected as surface at all, which is why the guard that computed the whole
# orphan family the day `executor.py` died said nothing about the one that killed
# 25 of 27 conversion producers. That blind spot now has its own unbaselineable
# check (`writerless_private_setters`), and it found two more within the hour.

# pgw#1425 — THIRTEEN ROWS WIRED AND TWO SENTENCED, 2026-08-18. All fifteen were
# appended wholesale by pgw#1373's `--write-baseline` (722e30b9, +79 rows) when
# `cd46c957` deleted `executor.py` and took every caller with it. That
# regeneration is what made pgw#1418 cost a rented pod to rediscover: the guard
# had already computed the answer.
#
# WIRED into the v2 serve path (`worker.py` / `serving/serve_loop.py` /
# `aot_serve.py`), so they leave this file by acquiring a production caller:
#   input_assets.materialize_input_assets — the payload seam, before the leases
#   stage_timing.{handler_open,handler_close,record_pre,stage_ms_for_metrics}
#   capability_renewal.renew_capability_while_running — per-job renewal task
#   postmortem.current_inflight_request — names the request on `aot_ingress_refused`
#   boot_phases.mark_once — sdk_ready / hello / first_request_servable / eager_ready
#   process_role.{declare,emit_boot_role} — at the transport bind
#   receipts.configure + content_credentials.configure_remote_signer — at HelloAck
#   serve_posture.apply_command — the ServePosture SchedulerMessage arm
#
# SENTENCED, with the ruling that kills them, because a row removed without a
# reason is the silence this issue exists to reverse:
#   serving_facts.facts_or_degrade
#   models.memory.report_unevidenced_serving_facts
# Both collapse a `SlotEvidence`, whose ONLY producer is
# `dispatch.order_from_binding`, which reads `objective`/`distilled`/
# `distilled_status` off a CATALOG-stamped `ModelBinding`. pgw#1373 DELETED the
# catalog/declaration architecture under Paul's hardcut mandate, so there is no
# stamp to read and `@entrypoint` declares no serving contract to check one
# against — producer and consumer both gone, in one ruling. The v2 successor for
# "this instance is serving degraded, and every request it serves says so" is
# `serving/placement.warn_if_degraded`, taken at residency-admit and stained onto
# every request through `ctx.warn` (`serve_loop._InstanceBackend.load`). The dead
# chain itself (`dispatch.order_from_binding` -> `SlotEvidence` ->
# `measured_posture.REASON_SERVING_FACTS_UNEVIDENCED`) is a DELETE that belongs
# with pgw#1425's remaining-69 triage, not smuggled into the P0 lane.

# pgw#1421's engine-hosted boot (PR #1016, `86d0cef9`) landed two callables with
# no production caller, an hour before pgw#1425 ran this ratchet. Rows added by
# the pgw#1425 lane rather than deleted, because they are NOT that lane's
# symbols and guessing another lane's intent is how a wire gets silenced — which
# is the whole subject of pgw#1425. They are OWED, not accepted:
#
#     OWNER:  pgw#1421. `LoadContext.engine()` is the accessor the engine-hosted
#             loader reads a slot's engine through, and `process_vram_bytes()`
#             is its measured-residency reading; both look like the seam
#             `serving/residency.py` is meant to size instances with. Wire them
#             there, or say which successor already does it.
#     EXPIRY: pgw#1421. If these outlive it, the engine-hosted boot shipped a
#             loader whose engine handle and VRAM reading nothing on the pod
#             ever asks for, and that is a finding rather than a lint row.
gen_worker.serving.context.LoadContext.engine()
gen_worker.serving.engine_runtime.process_vram_bytes()

# pgw#1425 (same commit): `receipts.configured()` is DELETED, not listed. It was
# the boolean `posture()` replaced, and it lost its caller in the fix itself —
# the ratchet caught it within the hour, which is the instrument working. Two
# spellings of one state is how a caller ends up asking a two-answer question
# about a three-state gate, and "not configured" collapsing `local` into `unset`
# is exactly the fail-open that issue closed.

# pgw#1485: `weightless_program.uninstall()` is DELIBERATELY test-only, and the
# ratchet is right that it has no production caller — it is the SUITE's half of
# a process-wide monkeypatch, not a runtime capability.
#
# `install()` replaces `torch.export.pt2_archive._package._load_state_dict` for
# the whole process and `mint_child` calls it. pgw#1468's two stock-path guards
# then measure whichever test ran before them on the same xdist worker: their
# fixture snapshot-and-restored a symbol that was ALREADY patched, so the red
# arm silently stopped being red. Both flipped to failing on a branch whose only
# change was test distribution. Restoring the pristine loader needs the closure
# the patch captured, which only this module can reach — so the alternative to
# this line is a test reaching into `__closure__` itself, which is worse.
#
#     OWNER:  pgw#1485. If production ever needs to unpatch (a worker that
#             serves after a mint child ran in-process), this acquires a real
#             caller and the ratchet will say so by going red on STALE.
#     EXPIRY: none. A guard's undo living beside its do is the correct shape.
gen_worker.serving.weightless_program.uninstall()
