# pgw#849 guard 2 — the RATCHET.
#
# Every line is public surface in the aot/mint/serve packages that NOTHING on
# the pod's production path calls, measured on chaos 9f582ba (2026-08-01).
# `scripts/lint_unreached_surface.py` fails on any NEW line, and equally on a
# line here that has since acquired a production caller — a baseline nobody has
# to shrink is a baseline nobody reads.
#
# These are not equivalent. Roughly, by what the absence COSTS:
#
#   A gate that never runs
#     numerics_ladder.gate / compare_outputs / declared_thresholds — ✅ DELISTED
#       2026-08-02, the first compiled graphs this ratchet has ever removed. The original
#       finding is kept verbatim because it is the clearest illustration of what
#       delisting MEANS: "pgw#817's ADOPTION numerics gate. `api/decorators.py`
#       declares numerics_floor and numerics_warn per family, `activity.py`
#       declares KIND_COMPILED_GRAPH_NUMERICS, and NOTHING IN src/ IMPORTS numerics_ladder
#       AT ALL. A compiled graph about to arm is supposed to be run against the eager
#       forward it replaces and REFUSE below the floor. It never is."
#       That sentence is now FALSE, which is the whole point — pgw#848 CP12
#       (`c47dc46`) gave all three their first production callers, the guard went
#       red on STALE, and the lines came out. A symbol ceasing to be dead code is
#       this guard's nicest failure mode, and the ratchet turned the right way.
#     compile_cache.assert_closure_complete — ✅ DELISTED 2026-08-08 (pgw#1035)
#       by DELETION, with closure_completeness_gap behind it. The original
#       finding: the code-closure completeness gate ("a dynamic import is
#       hiding trace-relevant code from the recipe key"), only caller a test.
#       pgw#990 had already demoted the closure from a key axis to a memo, so
#       the honesty this gate protected stopped being load-bearing before the
#       gate was ever wired.
#     aot_serve.is_aot_artifact — ✅ DELISTED 2026-08-08 (pgw#1035) by DELETION.
#       Its own docstring said "kind sniff for the provision.enable_compiled
#       dispatch"; that dispatch never called it.
#     aot_declaration.named_dynamic_rows — ✅ DELISTED 2026-08-08 (pgw#1030) by
#       DELETION. It bridged declaration rows that NAME a Dim into per-binding
#       DynamicDim rows; nothing ever ran it.
#
#   Measured and then dropped (the pgw#848 CP1 shape)
#     boot_phases.servable_ms — "THE cold-boot number", computed, never read.
#     aot_compile_spans.dark_fraction — unattributed compile time, never emitted.
#     aot_serve.realigned_inputs / served_compiled_graph_calls / CompiledGraphDispatch.refusal_counts
#       — the pgw#790/#791 adapter-fork and realignment tax, measured "rather
#       than inferred from a stderr line nobody can read", and never reported.
#       AUDITED FOR DELETION AND KEPT 2026-08-08 (pgw#1035): all three are the
#       built-but-UNWIRED class, whose fix is one caller carrying all three
#       (a JobMetrics or activity field), not three diffs that hide the gap.
#       `served_compiled_graph_calls` additionally has a real consumer — the mint rig's
#       vehicle table, driven by scripts/rig_gauntlet.py.
#     compile_cache.artifact_fx_lines / live_fx_lines — ✅ DELISTED 2026-08-08
#       (pgw#1035) by DELETION. fx forensics that fx_key_forensics does not
#       reach: the live divergence report builds its own line dicts from
#       `_fx_compiled_graph_lines`, so these two were a second way to do the same thing
#       that only the test ever took.
#
#   Dead or duplicated surface
#     compile_cache.build / prepare (the JIT build path) — ✅ DELISTED
#       2026-08-08 (pgw#1035) by DELETION, 259 lines. `build` was the
#       whole-pipeline dynamo mint superseded by aot_mint; `prepare` was a
#       swallow-everything wrapper over `seed_artifact`.
#     aot_serve.flavor_label and find_artifact — ✅ DELISTED 2026-08-08
#       (pgw#1035) by DELETION (compile_cache's same-named versions are the
#       live ones). `torch_maj_min` went with `flavor_label`, its only caller,
#       and `is_aot_ref`'s `aot-` label branch went with the label's only
#       producer.
#     aot_package.package_compiled_graph_names, guard_closure.manifest_digest.
#     aot_serve.unpack_metadata — ✅ DELISTED 2026-08-10 (pgw#1098) by
#       DELEGATION to `artifact_meta.read_metadata`. It was listed 2026-08-08
#       (pgw#1035) as "the AOT lane's own reader of its own packed envelope",
#       with the dedup deferred to the TRT-engine ratification on the reasoning
#       that waiting cost nothing. It cost $1.584 and a 92-minute sdxl mint:
#       pgw#1013 bounded the SHARED reader and not this one, so two readers of
#       one `metadata.json` disagreed about row 7's envelope — the bounded one
#       refused it, this one read it fine, and the asymmetry surfaced as a
#       LoRA-contract refusal with no root. `trt_engine.unpack_metadata` had
#       already been collapsed; this was the last of the eight.
#     compile_cache.seed_artifact — LISTED 2026-08-08 (pgw#1035). It became
#       caller-less when `prepare` was deleted, and it is deliberately NOT
#       deleted with it: it is one compiled graph point of the delivered-inductor-cache
#       ADOPT LANE (stage -> verify in isolation -> activate under the lock).
#       RE-VERIFIED after pgw#1032 MERGED (`ddf8b8e3`), which changed the facts
#       this note was first written against: the ADOPT_COMPILE_CACHE handler is
#       now GONE, and `arm_staged_artifact` went with it. What survives is the
#       BOOT path — `enable` -> `stage_artifact` -> `artifact_drift` ->
#       `_activate_staged` — which is still reached, so the lane is NOT dead
#       code and must not be deleted as such. The open question is narrower and
#       unchanged: whether anything still PRODUCES a delivered
#       `torch-inductor-cache` artifact for that path to consume. That is the
#       ratification Paul owes. Three real-compile proofs drive the
#       transaction through THIS compiled graph (compiled graph portability gw#611, adopt
#       collision pgw#751, executor adopt); routing them at `stage_artifact` +
#       `_activate_staged` would put tests on module privates to buy nothing.
#       It goes when the lane goes, or it gains a caller when the lane stays.
#       aot_mint.replace_spec — ✅ DELISTED 2026-08-05 by DELETION. It was a
#       one-line wrapper over `dataclasses.replace` that existed, per its own
#       docstring, "so the deferred-import dance does not have to repeat at
#       every call site". Hoisting the import removed the dance and the wrapper
#       with it: a deferred import had grown a whole public function.
#       aot_compile_pool.free_disk_bytes — ✅ DELISTED 2026-08-02 (pgw#877) by
#       DELETION, the other way a line leaves this file.
#     aot_export_parallel.groups / width_for — ✅ DELISTED 2026-08-02 by
#       WIRING (`5923622`, pgw#868 A4), then the MODULE DELETED 2026-08-08
#       (pgw#1030): the "wiring" fed a report, not a decision — export stayed
#       unconditionally serial and nothing consumed `decide()`'s six floats.
#       The 2026-08-02 note asked for the guard to be taught to see "a module
#       with NO importer at all"; this deletion is that lesson applied by
#       hand.
#
#   ⚠️ KNOWN BLIND SPOT — the wrapper exemption clears the WRONG function
#
#     MECHANISM: a definition is exempted when its body is a one-line call
#     into another symbol that production reaches. The reach test then runs
#     against the WRAPPEE, never the wrapper — so a wrapper with zero callers
#     of its own is cleared on the strength of the callers of the thing it
#     calls. The rule can be satisfied by a function that is not the one under
#     test, and the guard reports nothing.
#
#     MEASURED, 2026-08-02 (pgw#877): `aot_compile_pool.available_memory_bytes`
#     and `.free_device_bytes` had zero references anywhere in src/, tests/ or
#     scripts/ and were invisible to this guard for its entire life. Both are
#     now deleted. The same clearance used to cover
#     `aot_export_parallel.enabled`, whose "wrappee production already calls"
#     was `str.strip` / `dict.get` — stdlib methods, which every module reaches
#     and which therefore clear ANY one-line wrapper over them. That function
#     is gone too (pgw#1000 deleted the env gate), so the example is historical;
#     the exemption's shape is the point and it is unchanged.
#
#     KEPT, deliberately, and recorded rather than silently worked around: the
#     exemption is right far more often than it is wrong, and a rule tight
#     enough to catch these would re-list real delegating wrappers. What must
#     not happen again is treating its silence as proof of reach. Two candidate
#     tightenings, neither implemented: require the wrappee to be IN-PACKAGE
#     (kills the `str.strip` class outright), and flag any guarded module with
#     no importer in src/ regardless of per-symbol reach.
#
#   KNOWN FALSE POSITIVES (3 of 24 — the measured rate is 12.5%)
#     aot_inputs.inputs_for / latent_dims — declaration-author surface; the
#       callers are endpoint repos that vendor the wheel, like gen_worker.api.
#     boot_phases.BootSpan.skipped — reached by an operator script only.
#     They stay listed rather than exempted: an exemption rule broad enough to
#     clear them would also clear real hits.
#
gen_worker.aot_compile_spans.dark_fraction()
gen_worker.aot_inputs.inputs_for()
gen_worker.aot_inputs.latent_dims()
gen_worker.aot_package.package_compiled_graph_names()
gen_worker.aot_serve.CompiledGraphDispatch.refusal_counts()
gen_worker.aot_serve.realigned_inputs()
gen_worker.aot_serve.served_compiled_graph_calls()
gen_worker.boot_phases.BootSpan.skipped()
gen_worker.boot_phases.servable_ms()
#   shape_growth.coverage_line — ✅ DELISTED 2026-08-12 (pgw#1181), by the
#   ratchet turning the RIGHT way: it acquired a production caller and the
#   landing lane did not remove its row, which left `master` itself red on
#   this guard. Removed here because every PR inherits that red; the symbol
#   is not this lane's and nothing about it changed.
#
#   THE JIT PRODUCER DIED AND THE FORMAT FOLLOWED IT — ✅ DELISTED 2026-08-12
#   (pgw#1181) by DELETION, five rows in one cut, three of them listed by
#   pgw#1178 the same day with OWNER: Phase 1 and EXPIRY: the per-compiled graph atom.
#   Phase 1 traced the ~82 `compile_cache.pack` call sites and reported they
#   are not its tail: they exercise the JIT lane's OWN format, and format 3 is
#   one graph class keyed by `ek1` while `torch-inductor-cache` is a whole-compiled graph
#   FX-cache tarball with no per-class identity — so porting it is a category
#   error, not a task, and the format DIES. That re-assigned the three rows
#   back to this lane, and the expiry was discharged by deleting rather than
#   by waiting.
#
#   The original finding, kept because it is the clearest statement of what a
#   writer-less format looks like from inside the ratchet: pgw#1178 deleted
#   `compile_cache.mint_artifact`, the LAST producer of a
#   `torch-inductor-cache` artifact anywhere in this tree, and `pack`,
#   `artifact_metadata` and `guard_closure.closure_manifest` went unreached
#   the same instant. That answered the question the `seed_artifact` note
#   above had held open since 2026-08-08 — "whether anything still PRODUCES a
#   delivered torch-inductor-cache artifact for that path to consume" —
#   with: nothing does. The live adopt lane was consuming a format with no
#   writer, and had been for four days without anything going red.
#
#   `seed_artifact` itself is delisted here too, LISTED 2026-08-08 (pgw#1035)
#   and kept twice on the reasoning that the adopt lane it entered was still
#   reached. It was. The lane was reachable and the format was not writable,
#   which is a distinction this ratchet cannot see and a reader must.
#   `guard_closure.manifest_digest` — listed 2026-08-01, never explained —
#   digested the manifest `closure_manifest` produced, and went with it.
#

# ---------------------------------------------------------------------------
# IN FLIGHT — a seam that landed AHEAD of its caller. Not 2026-08-01 debt.
#
# Segregated deliberately. An exemption list that only ever gains compiled graphs is a
# guard being retired in slow motion, so compiled graphs below carry an owning issue and
# are expected to leave, not to accumulate. If one is still here when its issue
# closes, the seam was never wired and the guard was right the first time.
#
#   pgw#868 A4 (`d6067d2`, landed 2026-08-02) — the parallel-export seam. Both
#   symbols are the DECISION rules for out-of-process row export; the only other
#   mention of `width_for` anywhere in src/ is a COMMENT in aot_mint.py:1719
#   explaining why it exists. Caught by this ratchet within hours of landing,
#   which is the first time it has flagged live work rather than history.
#
#   (pgw#903's `expected_from_plan` compiled graph left with pgw#904: the RunAttempt
#   arming path now projects the Plan into the identity `stage_artifact`
#   verifies, exactly as the compiled graph instructed.)
# ---------------------------------------------------------------------------
# ---------------------------------------------------------------------------
# pgw#1182 — THE SCOPE IS NOW THE WHOLE PACKAGE, and this list grew from 14 to
# 107 because of it. Nothing below was newly written; it was always there and the
# fence was pointed at one seventh of the tree.
#
# Read the count as the SIZE OF THE QUESTION, not a backlog of confirmed dead
# code. Two things were subtracted before it: nine findings that were the
# guard's own resolver bug, and nineteen whose caller is an endpoint repo
# (`scripts/author_surface_allowlist.txt`, each with its call site). What
# remains is genuinely unreached FROM THIS TREE — which is the beginning of a
# question and not the end of one. Three dispositions, and the rule is the same
# one that governed the fourteen:
#
#   WIRE IT     the caller is missing and should exist. This is the defect
#               class (`compiled_graph_workers(peak_rss_bytes=…)`, `set_guard_failure_
#               callback`) and the reason the guard exists at all.
#   DELETE IT   with its tests, never porting them (DESIGN-RULINGS §4.34).
#   PROVE IT    the caller is outside this tree — then it belongs in
#               author_surface_allowlist.txt WITH the call site, not here.
#
# A line leaves this file only one of those three ways. `--explain` shows why
# anything absent was cleared.
# ---------------------------------------------------------------------------
# ---------------------------------------------------------------------------
# pgw#1192 — TYPES are enumerated now, and the cross-repo check follows package
# RE-EXPORTS. Both were blind spots and both were found the same way: by
# checking, not by reasoning. A symbol sweep over functions alone let a dead
# type survive a whole deletion pass on a sibling lane (only the compiler caught
# it), and requiring the DEFINING module path let nine live consumers sit in
# this file marked dead — `training-endpoints` imports
# `from gen_worker.convert import Dataset` against a class defined in
# `gen_worker.convert.dataset`.
#
# Compiled graphs are `module.Symbol` for a type and `module.symbol()` for a callable.
# ---------------------------------------------------------------------------

gen_worker.boot_key.assert_memo_honest()
gen_worker.boot_key.prop_economy()
gen_worker.component_vocab.ComponentVocabulary.role_of()
gen_worker.component_vocab.declare_components()
gen_worker.component_vocab.reset_component_vocabulary()
gen_worker.config.loader.unrecognised_owned_env()
gen_worker.config.process.current()
gen_worker.convert.base_model_families.declare_foreign_family_map()
gen_worker.convert.base_model_families.reset_foreign_family_maps()
gen_worker.convert.dataset.Dataset.as_dataloader()
gen_worker.convert.dataset.Dataset.is_prompt_corpus()
gen_worker.convert.dataset.Dataset.iter_rows()
gen_worker.convert.layout_converters.ConversionIO.set_metadata()
gen_worker.convert.layout_converters.classify_layout()
gen_worker.convert.layout_converters.conversion_provenance()
gen_worker.convert.layout_converters.register_layout_conversion()
gen_worker.convert.layout_converters.register_layout_production()
gen_worker.convert.layout_converters.registered_layout_conversions()
gen_worker.convert.layout_converters.registered_layout_productions()
gen_worker.convert.layout_converters.reset_layout_conversions()
gen_worker.convert.layout_converters.run_layout_conversion()
gen_worker.convert.loaded_component.LoadedComponent.save_to()
gen_worker.convert.registry.load_declaration_module()
gen_worker.convert.registry.register_layout()
gen_worker.convert.registry.register_repackage_family()
gen_worker.convert.registry.reset_registries()
gen_worker.convert.source.Source.as_hf_model()
gen_worker.convert.source.Source.iter_hf_components()
gen_worker.convert.source.Source.tokenizer()
gen_worker.convert.source.Source.weights_size_bytes()
gen_worker.convert.writer.fp8_te_components()
gen_worker.convert.writer.streaming_cast_snapshot()
gen_worker.convert.writer.streaming_w8a8_snapshot()
gen_worker.families.base.family_registry()
gen_worker.geometry.FamilyGeometry.assert_declared()
gen_worker.input_assets.inputs_dir_for_request()
gen_worker.io.exists()
gen_worker.io.read_audio()
gen_worker.io.write_image()
gen_worker.kernel_path.Verdict.measurement()
gen_worker.kernel_path.clear()
gen_worker.kernel_path.probe()
gen_worker.models.attention_modes.known_attention_modes()
gen_worker.models.chunk_upload.sources_from_declarations()
gen_worker.models.fp8_storage.castable_leaves()
gen_worker.models.hf_fp8_blockwise.dequantize_block_scaled()
gen_worker.models.hf_fp8_blockwise.load_hf_fp8_blockwise()
gen_worker.models.ladder.Placement.admits_sm()
gen_worker.models.lora_lifted.LiftedLoraBinding.swap()
gen_worker.models.lora_lifted.LiftedLoraPlan.views()
gen_worker.models.native_kernels.extension_available()
gen_worker.models.native_kernels.svdq_linear_execution_lane_reason()
gen_worker.models.native_kernels.svdq_modulation_execution_lane_reason()
gen_worker.models.residency.Residency.drain_shared()
gen_worker.models.residency.Residency.leased_refs()
gen_worker.models.residency.Residency.shared_refcount()
gen_worker.models.residency.Residency.shared_stats()
gen_worker.models.residency.Residency.transition_stats()
gen_worker.models.structure_only.StructureFacts.as_event()
gen_worker.models.svdq_layout.quantize_lowrank()
gen_worker.models.tensor_layout_contract.parse_layout_id()
gen_worker.models.volume_verify.clear_memo()
gen_worker.models.w4a4.quantize_tree_w4a4()
gen_worker.models.w4a4.sanitize_w4a4_state_dict()
gen_worker.parallel.cp._GroupMesh.get_group()
gen_worker.parallel.cp.refuse_unless_divisible()
gen_worker.procsplit.group.GroupPlan.child()
gen_worker.progress.reset()
gen_worker.progress.tracking
gen_worker.runtimes.llama.chat_deltas()
gen_worker.runtimes.llama.completion_deltas()
gen_worker.shape_growth.GrowthLedger.counts()
gen_worker.shape_growth.GrowthLedger.seen()
gen_worker.sparse_attention.build_block_mask()
gen_worker.sparse_attention.measured_density()
gen_worker.sparse_attention.probe()
gen_worker.sparse_attention.sparse_attend()
gen_worker.stage_timing.reconciliation()
gen_worker.url_fetch._NoRedirect.redirect_request()
gen_worker.utils.lora.AdapterResidency.stats()
gen_worker.warm_spans.WarmLedger.table()
gen_worker.worker_credential.expires_at()
gen_worker.worker_credential.reset()
gen_worker.worker_fatal.fatal_identity()
gen_worker.worker_goals.install()
