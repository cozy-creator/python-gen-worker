# pgw#1480 — phase-vocabulary members that NOTHING in src/gen_worker
# references. Generated by `scripts/lint_phase_enum_emitters.py --write`.
#
# Each line is a DEFECT, not an exemption: an instrument nothing can
# fire is one a proof condition can be written against and never
# fail (se#780 nearly spent an 80 GB rental on exactly that).
# They are listed so the gate can be green while the set is burned
# down, and the gate REFUSES to let this list grow or go stale.
#
# Wire-or-delete for each is a RULING, not a lint decision: the hub
# groups historical `worker_activity_events` by these values.
#
EagerPhase.ADOPTED_COMPILED_GRAPH_REFUSED
EagerPhase.ARMED_TARGET_UNRESOLVED
EagerPhase.ARM_AXES_UNSTATEABLE
EagerPhase.ARM_PENDING
EagerPhase.BOOT_ENDED_UNCOMPILED
EagerPhase.COMPILED_GRAPH_QUARANTINED
EagerPhase.HUB_ORDERED_EAGER
EagerPhase.JIT_ARM_FAILED
EagerPhase.KEY_COMPUTATION_FAILED
EagerPhase.MANDATORY_LANE_NEEDS_A_COMPILED_GRAPH
EagerPhase.MINT_IN_PROGRESS
EagerPhase.MINT_UNAVAILABLE
EagerPhase.NO_COMPILE_CANDIDATES
EagerPhase.NO_COMPILE_DECLARED
EagerPhase.NO_COMPILE_TARGET
EagerPhase.NO_FAMILY
EagerPhase.NO_TOOLCHAIN
EagerPhase.UNCOMPILED
