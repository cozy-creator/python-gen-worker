- **pgw#1665: the vendored tensor-layout corpus moves to tensorfs `07f9615`, and a SUPERSEDED handle major now RESOLVES at the declaration boundary instead of refusing the whole fleet.**
  tensorfs#153 does not add `@2` beside `@1`; it REPLACES the document. Nine topology records
  (`sdxl.diffusers`, `sd15.diffusers`, `sd2.diffusers`, `sdxl-inpainting.diffusers`,
  `sdxl.clip-g-fused`, `wan22.diffusers`, `ernie.diffusers`, `ltx2-upsampler.diffusers`,
  `z-image.diffusers`), the `cozy.fp8-rowwise` quant rule and the
  `sdxl.clip-g-split-to-fused` morphism moved that way — so on a literal membership test every
  `lanes={("sdxl.diffusers@1", …)}` and every committed `endpoint.lock` refuses at CLASS
  DEFINITION, before author code runs and identically under `gen-worker lock --check`.
  `resolve_declared_handle` is th#2301's hub-side `ResolveDeclaredContract` applied to the other
  end of the same wire: an exact member is answered exactly (`anima.net@1` and
  `minimax-h3.diffusers@1` survive beside their new `@2` and are never "upgraded"), a superseded
  major resolves by name, and a major AHEAD of this corpus refuses TYPED
  (`ContractVersionAheadError`) rather than being read as the older layout it is not.
  **Nothing is rewritten**: `parse_lane_stamp` still answers the author's spelling, so the
  rendered `contract_id` — a derivation input of the graph identity — does not move under a
  corpus bump and no artifact re-keys. The bind door gets the same treatment as hub-side: the
  decode-set intersection compares the DESCRIPTOR DIGEST two handles resolve to, so an image
  declaring `cozy.fp8-rowwise@1` still reads a checkpoint the hub's resweep has already moved to
  `@2`. Resolution onto a document whose own digest disagrees with the corpus ledger REFUSES —
  the one substitution this repo makes has to be a ratified one. Also carries tensorfs#161
  (`sensenova-u1.mot@1`, which pgw#1664 is blocked on) and turns the vendored-corpus drift check
  from a SKIP into a REFUSAL when the pinned rev is not a commit upstream: a mistyped 40-hex sha
  re-pinned the whole snapshot and the check reported green by not looking.
