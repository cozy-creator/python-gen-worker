- **pgw#1187: TensorRT is removed entirely, per Paul (2026-08-12); revival is a REWRITE, not a
  revert (tracker `python-gen-worker/future.md`, "TensorRT, reconsidered", `2b60cbc1`).** Paul:
  *"for now, I think you should completely remove TRT; you go ahead and cut all of it"* — and on
  keeping the code, *"we never actually tested it much anyway... deleting it now and recreating it
  later is probably the best move."* **This single entry supersedes pgw#1179's TRT paragraph and
  the pgw#1180 restore (PR #716) both** — the restore existed only so this cut could land from a
  consistent master, and the two should read as one event, not three. **0.113.0's release note
  called this exactly right**: *"Whether `produce-trt-engine` retires WHOLE is a Paul-level
  decision expected to land in 0.114.0."* It did, and this is it — 0.113.0 shipped
  `trt_engine.build()`, and 0.114.0 ships no TensorRT at all.
  **The ground for retirement is USAGE, not the ie#513 benchmark.** Measured read-only on the
  standing stack 2026-08-12: **0 of 326 requests name trt**, **no endpoint binds any trt flavor**,
  and the conversion endpoint's only invoked function is `clone-huggingface`. `produce-trt-engine`
  was a **priced product function that had never been invoked once**. ie#513's LTX-VAE numbers
  (TRT bf16 2.25 s vs a 1.46 s compiled arm, on a B200) are scoped to bf16 on one conv-heavy net,
  are TRT's weakest mode against its worst-case net, and are **not** cited as a general verdict —
  INT8, TRT's winning mode, was never testable on cu13 at all.
  DELETED here: `src/gen_worker/trt_engine.py` (971 lines — builder, ONNX export helpers, refit
  map, `TrtModuleRunner`, the whole serve half) and `tests/test_trt_engine.py` (393); the serve-half
  call sites in `executor.py` (the `trt_execution_before` ledger, the third proof counter, the
  unproven-TRT unwrap block, `is_engine_ref`/`is_armed`/`unwrap`/`set_guard_failure_callback`
  branches), `models/provision.py`'s `trt-engine` kind arm, `compile_cache.can_swap`'s TRT probe,
  and `models/hub_policy.py`'s `tensorrt` + `tensorrt==<version>` `installed_libs` advertisement
  (th#575) — the hub has nothing left to resolve against it.
  **Tests: 3 deleted, 1 consolidated.** The two `trt_engine.enable` rows in
  `test_error_visibility_pgw760.py` and `test_the_trt_cell_predicate_still_reads_its_fragment` die
  with their subject and are not ported (§4.34); `test_provision_still_routes_trt_and_inductor_kinds`
  is narrowed to `test_provision_still_routes_the_inductor_kind`, which is the half that was ever
  live.
  Landed as one coherent cross-repo bundle in dependency order so that **at no commit does a caller
  exist without its callee**: e2e#275 (the priced binding) -> te#184 (`conversion/trt_engine.py`,
  the only caller anywhere of `trt_engine.build()`) -> **pgw#1187** -> th#1845 (the hub's ref
  predicates and `tensorrt` backend).
