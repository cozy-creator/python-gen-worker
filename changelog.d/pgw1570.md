- **pgw#1570: an offload rung no longer turns OFF flash attention.** `model_offload`,
  `group_offload` and `sequential` were arming `enable_attention_slicing()` and
  `enable_vae_tiling()` as a side effect of moving weights. diffusers turns the first into a
  `SlicedAttnProcessor` — `baddbmm`+`softmax` in a python loop over `batch*heads` chunks,
  full NxN scores materialized — **in place of** `AttnProcessor2_0` (torch SDPA →
  flash/mem-efficient), so every denoise step of every request paid a chunked-math tax for
  the life of the process. Measured A/B/A on an RTX 4070 Laptop, SDXL 1024², 20 steps, CFG,
  bf16, eager: **1.10 s/step sliced → 0.827 s/step with SDPA (1.33x), warm round trip 26.1 s
  → 23.6 s, and peak VRAM moved 5932 → 5956 MiB — the entire tax bought 24 MiB.** A placement
  rung now answers only where the weights live; pgw#1499's `oom_ladder`, already armed on
  every rung, applies both savers reactively when an op actually does not fit. The `cpu` rung
  keeps them up front, because host RAM raises no CUDA OOM to react to. Finishes the cut
  th#1107 started at `vae_only`.
- **pgw#1570: the offload confession now NAMES THE ATTENTION KERNEL it left running.**
  `DEGRADED_MODE=engaged …` carries an `attention=<Processor>x<count>` census read off the
  denoiser's live `attn_processors`. The flag that set a kernel is not the same fact as which
  kernel runs, and only the second one is measurable.
