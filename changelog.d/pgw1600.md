- **tensorfs vendor bump to `1da68d58` (tensorfs#130): nine lane documents land, and 9 of the 11 Model classes pgw#1599 refuses can now declare.** `anima.diffsynth-bf16`, `joycaption.llava-bf16`, `krea-2.diffusers-bf16`, `ltx-2-upsampler.diffusers-bf16`, `musicgen.transformers-fp16`, `qwen3.6-35b-a3b.vllm-fp8`, `rife.flownet-fp32` (ONE document for the three classes that used to declare the same `Rife` type three different ways), plus `qwen3.6-27b-mtp.gguf-ud-q4-k-xl` and `sdxl.diffusers-nvfp4-flat`. **Two classes are still blocked and neither is waived:** `qwen3.6-27b-mtp-gguf`, because GGUF is a quant CONTAINER with per-tensor ggml types and no single top-level dtype, so the declaration refuses it (tensorfs#127's lane-vs-fragment format gap is what closes it); and `hunyuan3d-2.1`, whose repo is pickle-only and needs a pod-side repack. `DTYPELESS_UPSTREAM_LANES` stays EMPTY — a waiver there knowingly accepts a lane whose serve-side `ctx.lane.dtype` raises on a rented pod, and a blocked endpoint is a better state than a green declaration that dies at load. `Contract.recipe` and `Contract.verdict` are DELIBERATELY not ported: both delegate to the Rust core, and porting them would put a second implementation of a bind ADMIT decision in this repo — the hazard upstream's own docstring names. The bind verdict is the hub's gate, reading the one implementation.
- **All nine of tensorfs#130's new documents declare themselves `GENERATED CANDIDATE - NOT RATIFIED`, and a test now pins that.** tensorfs#130's design is *generate → human-ratify → publish* — that is what makes a contract cheap without making it a guess. These nine are generated and published; the ratify step is outstanding. Vendoring them is harmless (pgw ships no `lanes=` naming any of them, so no endpoint is bound to an unchecked layout) but **se#816 must not adopt one until it is ratified**: a generated contract is a hypothesis shaped exactly like a ratified one, and the bind gate cannot tell them apart — it will refuse a real checkpoint, or admit a wrong one, with equal confidence. `test_the_generated_candidates_declare_themselves_UNRATIFIED` is EXPECTED to go red when the marker is dropped; the day it fails is the day se#816 is unblocked. Separately, the pgw#1512 denoiser fence was NARROWED by a case that falsifies its premise rather than inconveniencing it: `musicgen.transformers-fp16` leads patterns with `text_encoder.` / `audio_encoder.` / `decoder.` because `MusicgenForConditionalGeneration` is a SINGLE-FILE checkpoint whose state dict genuinely carries those submodule prefixes — there is no directory to have confused them with. The exemption is by FORMAT segment (`transformers`), not by filename, so a `diffusers` contract leading with a component word is still an offender, and a second exemption has to be decided rather than absorbed.
