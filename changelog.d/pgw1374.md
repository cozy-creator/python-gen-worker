- **Paul's comment-purge mandate reaches `gen-worker`: 463 comment lines removed from `src/`,
  `scripts/` and the two workflow files.** Historical narrative — why a thing used to be different,
  what a since-deleted lane decided — is release history, not code, and it had accumulated to
  17,675 own-line `#` comments across `src/gen_worker` alone. Behaviour is untouched: the sweep is
  comment-only and AST-identical, `tests/` and `tests_v2/` were skipped whole (pgw#1362's folds
  rewrite those comments anyway), and the load-bearing markers (`# noqa`, `# type: ignore`,
  `# tcg-vocab:`, `# fence-symbol-exempt:`, `# pickle-ban:`, `# oci-image:`, `# refused:`,
  `# mixed-cas-hatch:`, `# pragma:`) plus the two byte-matched contract files were carved out
  before the first edit rather than restored after a red.
