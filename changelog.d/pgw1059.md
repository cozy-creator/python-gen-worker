pgw#1059: the ck1 KEY-SCHEMA REDEFINITION (Paul-directed). The cell key is
exactly `graph × envelope × sm × toolchain` — the traced computation
(`combined_graph_hash`, one derivation, proven at admission), the declared
serving region (`declared_envelope` block, renamed from `declared_traffic`;
empty behavior-posture overlay slot), the GPU architecture, and the compiler
stack as we configure it (binaries + the pgw#1049 settings-declaration digest
+ loaded-libs digest folded into `toolchain`). Dropped from the key: `kind`/
`format` (single-valued), `family`/`lane` (store metadata + discovery scoping
only), `env_seal` (folds into `toolchain`; the boot-verify and pre-trace
tripwire GATES are unchanged, and the seal digest still rides the published
identity-axes map for the hub's ArtifactIdentity). The scheme prefix stays
ck1 (pre-launch redefinition + corpus purge, per §1.27(g) precedent — no
ck2). The pre-trace "computed key" is retired: a mint obligation is named by
`fleet_cells.arm_identity` (`arm1-…`, never IsCellKey-shaped), and the
cozy-local/seeded verdict is `compile_cache.local_cell_mismatch`
(fact-by-fact, refusals named). Admission now proves the stamp:
`aot_serve.verify_contract` recomputes the key from the artifact's own
recorded facts, so forged stamps and pre-redefinition cells are refused
structurally. Membership-axiom test + one-derivation fence:
`tests/test_cell_key_pgw1059.py`.
