- **pgw#1277: compiled-graph IDENTITY moves to TCG, and the worker keeps the
  facts that were never identity.** `src/gen_worker/compiled_graph_key.py` is
  DELETED. `KEY_SCHEME`, `CompiledGraphKey`, `is_key`, `from_axes`,
  `toolchain_axis_digest` and `from_entry_metadata` are now
  `tcg.identity.*` / `tcg.is_compiled_graph_key`
  (`from_entry_metadata` → `from_artifact_metadata`, which also enforces what
  `EXPORTED_KIND` used to check), and `CompiledGraphKeyError` is
  `tcg.identity.IdentityError`. Importers update — there are no aliases.

  What remains was never identity, so it moves to the new
  `gen_worker.graph_facts` under the name of what it is: facts about a
  DECLARATION and an OBLIGATION — the declared envelope, the contract-manifest
  coverage label, the slot subject and the canonical fact digest. `ENTRY_BLOCK_KEY`
  becomes `graph_facts.TCG_GRAPH_CLASS_BLOCK` (`'graph_class'`), the block TCG
  actually writes; the worker had been keying one TCG never writes. The two
  constants TCG does not export stay worker-side as PROJECTIONS, fenced against
  TCG's own refusals rather than against a second copy of the guess.

  Equivalence was proven BEFORE the swap, not asserted after: all 26 shared
  corpus vectors answer identically through TCG's grammar, `from_axes` agrees
  digest-for-digest, and `toolchain_axis_digest` agrees on a block carrying an
  evicted component.
