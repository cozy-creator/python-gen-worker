- **pgw#1303: the file-access preference ladder, and the llama.cpp runtime's two silent projection defects.** Paul ruled the materialization seam PERMANENT and NOT separately priced — it is simply tier 3 of a fixed ladder: (1) symlink snapshot + native tensorfs reads, (2) symlink snapshot + FUSE *where FUSE exists* (not on a RunPod pod), (3) `materialize()`. `models/materialized_view.py` loses its pricing framing and keeps its per-copy byte+caller logging, which is now the migration burn-down evidence. The hatch is spelled `materialize()` everywhere (tensorfs#107 renamed `extract()`), and `scripts/lint_materialization_hatch.py` REFUSES the retired `.extract(` spelling instead of accepting both. Two live tier-3 defects fixed in `runtimes/llama.py`: `llama-server -m` was handed the projected stub path rather than a real file, and `read_gguf_info().size_bytes` summed `st_size` over stubs — ~128 bytes for a model of any size, so every layer "fit", `-ngl` went to full offload and the degraded-boot ladder never engaged. Neither failed loudly.
