- **pgw#1272 (HARDCUT-LEDGER Phase 1): 47 test modules deleted, and the
  finding that the phase's premise was wrong for this repo.** Tensorhub's
  Phase 1 (th#1979) cut 823 of 1,075 test files because they were mock-heavy.
  The same audit run over this repo's 502 files says the opposite: **42 files
  (7% of the tree) are mock-heavy; 273 (61%, 86k LOC) drive real subprocesses,
  real gRPC over TCP, real loopback HTTP, real gloo process groups, real
  `torch.export`/AOTI and real safetensors on disk.** The 0.96 test:src ratio
  is integration coverage, not bloat. Deleted: the 41 mock-heavy units, 5
  deletion fences for hard cuts that already completed (flavor-as-address
  th#1803, the computed key space pgw#1059, forge/mint-request pgw#1092, the
  VRAM marker vocabulary th#1867, the Plan head th#1842), and one fixture
  orphaned by the cut.
- **pgw#1272: no fence was deleted as a lint duplicate, because none is one.**
  All 21 `fast gates` guard scripts were checked against the 59 fence tests
  they plausibly duplicate. Every pair asserts different things — e.g.
  `lint_config_reads.py` is an AST census of `os.environ` sites while
  `test_config_boundary_pgw931.py` asserts the loader's runtime refusals, and
  `test_behaviour_gate_visitor_th1887.py` is the only red/green exerciser of
  that lint's own gate scanner. Deleting it would have been the pgw#1176
  "guard rots green" failure this repo has already measured once.
- **pgw#1272: the mypy ratchet moved with the deletions, not after them.**
  27 stale `tests.*` `ignore_errors` entries pruned from `pyproject.toml` and
  `HIGH_WATER["ignore_errors"]` lowered 295 → 268, so the ground is held.
