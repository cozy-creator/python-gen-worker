- **pgw#1273 (SECURITY): the Hugging Face lane could select and LOAD a pickle
  weight — it now refuses.** The pickle ban is absolute (reading IS the banned
  act) and the HF direct-download path had no refusal anywhere: `_WEIGHT_SUFFIXES`
  admits `.bin/.pt/.pth/.ckpt`, `_pick` falls back to the whole component group
  when it holds no safetensors and so SELECTS the pickles, an unrecognized layout
  returns the whole repo, and `use_safetensors=True` appears zero times in the
  tree — so a selected `pytorch_model.bin` reached `from_pretrained` and was
  unpickled in the process holding hub credentials. The tensorhub lane already
  refused on the resolved manifest and the civitai lane is a clean allow-list;
  only HF had nothing. Two layers, both refusing before or instead of a load:
  a **selection-time** refusal at the one chokepoint that sees the final resolved
  set across all three paths (`files=` patterns, `select_hf_files`, narrowed
  listing), so no byte moves; and a **materialized-directory** refusal for
  `local_files_only`, which returns before that chokepoint and is followed
  through symlinks because the HF cache materializes snapshots as links into
  `blobs/`. The selector is deliberately unchanged — narrowing `_pick` would
  fetch a component with no weights and fail later and further away, while
  refusing names the file. `PICKLE_WEIGHT_EXTENSIONS` and
  `first_pickle_weight_path` now live once, beside `PickleWeightRefused` in
  `models/errors.py`; there had been five copies and they had already drifted —
  `convert/writer.py` carried four entries where every other copy carried six,
  so a `.pkl`-only component yielded zero tensors instead of raising.
