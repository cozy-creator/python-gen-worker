- **pgw#1301 / te#220: `Source.iter_hf_components` is deleted — the bnb conversion
  plumbing had one caller, and that caller is gone.** The method existed to serve
  training-endpoints' `bitsandbytes_quantization` (te#220 deleted it: a producer whose
  nf4/int8 output no pgw ladder class and no hub precision class could even name), and
  a fleet-wide grep found no other production caller — modelopt/svdq/casts all go
  through `as_hf_model()` or raw `source.path`. With it go `LoadedComponent`
  (`convert/loaded_component.py`), `_hf_load.py`, both `_iter_*_components` walkers,
  and — closing pgw#1301's checklist — the `offload_folder` / `device_map="auto"`
  branch whose docstring advertised disk-spill behavior nothing could ever reach
  (the sole caller passed `offload_folder=None` explicitly). The serving-side reader
  for PRE-QUANTIZED third-party bnb uploads (`models/loading.py`
  `synthesize_quantization_config` and the nf4 dtype vocabulary rows in
  `dtype_pins.py`/`clone.py`) is deliberately untouched: that keep-or-kill is a
  separate Paul decision (te#220 checkbox 3, cross-ref pgw#1303).
