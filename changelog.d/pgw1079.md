- **pgw#1079 / ie#637: `micro-pad32` — the pad-to-32 expression class, answered.** ie#637's
  surviving watch item was whether AOTInductor CODEGENS `32*FloorDiv(L+31,32)` — a pad-to-32 over a
  token count that is itself a PRODUCT of two declared dynamic symbols. Two z-image pods died
  before that phase (the declared-range gate, then ie#638's VRAM), so the question cost a rented
  A100 to ask. Two new gauntlet members ask it on a 1.1 MB model in minutes. **Answer: it codegens
  correctly.** On the GPU lane (RTX 4070, sm_89, torch 2.13.0+cu126, real AOTI — `triton_s=10.73`,
  `autotune_s=1.95`, `host_compile_s=51.73`) ONE collapsed artifact served L = 144 (pad 16), 196
  (pad 28, an UNDECLARED row served from the derived range) and 256 (pad 0) at parity **5.96e-07**
  on every row; the cardless leg agrees at 9.54e-07. Remedy (c) — static-per-row, 10 artifacts per
  CFG arm — is not needed.
- **`micro-pad32-branchy` is the control, and it is expected GREEN.** Upstream's branching pad
  spelling is NOT refused on 0.96.3+: its shape env carries ie#637's `Eq(PythonMod(-L, 32), 0)`
  recorded **REFUTED**, which is exactly what pgw#1077 taught the declared-range gate to evaluate
  rather than count, and the resulting artifact serves all three rows correctly. A RED there means
  the gate started over-refusing again. `tests/test_pad32_codegen_pgw1079.py` pins the guard
  algebra of both spellings plus their value-equality with the unpadded base at pads 0/16/28.
