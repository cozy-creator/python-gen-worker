- **pgw#1609: a derive that meets a projected tree stops reporting a corrupt checkpoint.** `gen-worker lock` hands the author's own `from_pretrained` the tree `cli/workspace.resolve_checkpoint` returned — which `gen-worker download` PROJECTED, so every `.safetensors` in it is a ~128 B `TFSSTUB1` pointer stub. An author loader that opens one with the stock reader gets `SafetensorError: header too large`, a sentence about a checkpoint that is intact. `serving.context.LoadContext` has guarded that since pgw#1513; `TraceLoadContext` — the one `ctx.load` that runs the AUTHOR'S loader against a real hub tree — had nothing to say about it, and se#817 paid for the gap on a rented pod. The trace half now wraps the loader's failure in `ProjectedTreeAtTrace`: the original exception survives verbatim as the `__cause__`, and the stub census plus pgw#1303's access ladder are added to it. A DIAGNOSIS and not a refusal, deliberately — a pre-check cannot tell a loader that reads stubs correctly from one that does not, and a tree of real bytes raises exactly what it raised before.
