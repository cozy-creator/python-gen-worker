- **pgw#1181 (th#1834 P0-D, the tail): the `torch-inductor-cache` FORMAT is deleted — net −2,861 lines.** pgw#1178 removed `mint_artifact`, the last thing in this repository that could WRITE the format, and left `pack`, `artifact_metadata` and `guard_closure.closure_manifest` baselined as production-unreached with OWNER: Phase 1. Phase 1 traced the ~82 `compile_cache.pack` call sites and reported they are not its tail — they exercise the JIT lane's own format, and format 3 is one graph class keyed by `ek1` while this is a whole-cell FX-cache tarball with no per-class identity, so porting it is a category error. The format dies instead.
- **`compile_cache.enable` keeps its arming half and loses the artifact half.** It is live production (`models/provision.py`) and is what kept most of the "dead" surface reachable, so it had to be dealt with before the deletion rather than after: it now takes `(pipeline, cfg)` and no `cache_dir`/`artifact`, because everything delivered is dispatched on `metadata.json`'s `kind` upstream of it and what reaches it is the JIT lane. The w8a8/w4a4 fence — the only wire-visible diagnostic a serve pod has — survives with its one remaining cause. Gone with the artifact half: `stage_artifact`, `_activate_staged`, `_merge_staged_cache`, `_StagedArtifact`, `seed_artifact`, `unpack`, `verify`, `pack`, `artifact_metadata`, `capture_env`/`seed_env`, `artifact_drift`, `contract_drift`, `mode_drift`, `execution_lane_drift`, `local_cell_mismatch`, `composition_fingerprint`, `_reconcile_resident_mode`, the cubin/PTX completeness gates and `ARTIFACT_FORMAT`'s tarball members.
- **The guard manifest went with the cell that carried it.** `closure_manifest` wrote the classification into cell metadata; `assert_posture` read the seal back at arm; `load_manifest`, `consolidate`, `FleetAudit`, `manifest_digest` and the `python -m gen_worker.guard_closure` CLI compared those manifests across N pods. With no writer, `load_manifest` could only ever raise *"carries no guard manifest"* — a tool that could not succeed. **KEPT and untouched: the guard-miss doctrine** (`extract`, `audit_armed`, `ClosureReport`, `canonical_ingress`, `GuardBoundaryError`, `_record_guard_miss`), `establish_posture`, `posture_snapshot`, `CANONICAL_POSTURE` — all of which are live and none of which needed the cell.
- **All five baselined rows are delisted BY DELETION**, including `seed_artifact` (listed 2026-08-08, kept twice on the reasoning that the adopt lane it entered was still reached — it was; the lane was reachable and the format was not writable, a distinction the ratchet cannot see and a reader must) and `guard_closure.manifest_digest` (listed 2026-08-01, never explained).
- **Test side, §4.34, reported as two numbers.** **DELETED: 65 rows**, 15 of them as four whole files whose subject IS the format — `test_adopt_collision_pgw751` (the seed-time cache-merge collision), `test_pack_refusal_forensics_pgw733` (pack's cubin gate), `test_cell_portability_gw611` (the capture/pack/seed cross-process round trip) and `test_guard_gate_advisory_pgw756` (the advisory mint classification; its one live row, `canonical_ingress` dtype drift, was already covered better in `test_guard_closure_pgw681`). **RETARGETED: 61 rows**, because the counterweight is binding and this program's dominant defect is correct code nothing calls. Four `_artifact`/`_cell_artifact`/`_cell_snapshot` fixtures move onto the exported `aot-inductor` cell through the shared `tests/harness/cell_meta` — one fixture change each, keeping w8a8 lane proofs, warmup attribution, compile alarms, boot deferral, desired-hot arming and partial-shape coverage on the format this repository can actually write. Individually retargeted: the receipts gate observes its drop at the kind dispatch; guard classification reads `audit_armed` instead of the mint wrapper; composition drift reads `execution_contract`, which `ck1` folds in, so a drifted consumer gets a different KEY rather than a named refusal; the autograd-cache pin asserts at its owner `settings_authority`; pgw#686's burst rows assert on the lane label, which is what the divergence actually was.
