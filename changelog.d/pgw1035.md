- **pgw#1035: serve-lane dead code, wave 2 — ~410 lines proven dead by
  consumer census, plus the §4.24 rows two pool constants owed.** From
  `compile_cache` (359 lines): `build` (226 — the whole-pipeline dynamo mint
  superseded by `aot_mint`, whose artifact kind `aot_cells.discover` rejects
  anyway), `prepare` (a swallow-everything wrapper over `seed_artifact`),
  `closure_completeness_gap` + `assert_closure_complete` (pgw#990 demoted the
  code closure from a key axis to a memo, so the honesty this gate protected
  stopped being load-bearing before the gate was ever wired),
  `artifact_fx_lines` + `live_fx_lines` (a second way to build the forensics
  dicts the live divergence report builds itself from `_fx_entry_lines`), and
  the three test-only accessors `guard_miss_count`, `in_tenant_serve_window`,
  `process_compiles_disabled`. `static_code_closure` lost its `roots` parameter
  with the last caller that passed one. From `aot_serve` (51 lines):
  `flavor_label` and `torch_maj_min` with it, `is_aot_artifact`, its private
  `unpack_metadata` caller aside, `find_artifact` — `compile_cache`'s
  same-named versions are the live ones — and `is_aot_ref`'s `aot-` label
  branch, whose label form had exactly one producer (`flavor_label`, no
  callers), leaving ONE recognizer: the stamped keys pgw#1033 requires every
  envelope reader to register. **§4.24 rows now paid:** `MAX_ENTRY_WORKERS = 8`
  was defended by twenty lines about RETIRED regional mints and a deleted
  function; it is re-derived against a named threat (K children compiling into
  one shared inductor cache) and a stated falsifier (a family where ceil(N/K)
  strictly decreases past 8). `DEFAULT_ENTRY_PEAK_RSS_BYTES = 3 GiB` was
  accused of contradicting pgw#877 #5's refuse-to-widen ruling; it does not —
  that branch fires on a genuinely UNKNOWN footprint, this one carries a
  MEASURED 2.09 GiB at 1.43x margin, and a host overshoot lands in a 4 GiB
  reserve where a device overshoot is a hard CUDA OOM. Refusing to widen here
  would pin K=1 on every cold pod's first mint and buy nothing. **The second
  posture vocabulary is pinned:** `serving_mode.POSTURE_ARM_PENDING` /
  `_NO_COMPILE_DECLARED` / `_UNCOMPILED` and the executor's
  `boot_ended_uncompiled` rode the same wire columns as `EagerPhase` while
  living as bare literals — the exact two-lists-of-literals drift channel that
  enum exists to close. They are `EagerPhase` members now and the `POSTURE_*`
  names are aliases; values are unchanged, so the hub's grouped history is
  untouched. **Audited and KEPT, with reasons recorded in the ratchet
  baseline:** `compile_cache.seed_artifact` (the delivered-inductor-cache adopt
  lane's entry point — that lane's fate is a ratification, and three
  real-compile proofs drive the transaction through it),
  `aot_serve.unpack_metadata` (the AOT lane's own envelope reader, driven by
  the pgw#699 double-mint byte-compare; its byte-identical `trt_engine` twin is
  the TRT ratification's to resolve), `aot_serve.combined_graph_hash` (LIVE —
  it is the survivor pgw#1030 chose) and `realigned_inputs` (built-but-UNWIRED,
  not dead: deleting one of three sibling ingress measurements would hide the
  pgw#791 tax while leaving the machinery that computes it). The
  `lint_unreached_surface.py --all` `RecursionError` this issue asked to fix is
  NOT reproducible on master `70796357` — `--all`, `--all --params`, `--params`
  and `--explain` all exit 0 on CPython 3.12.
