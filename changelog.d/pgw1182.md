- **pgw#1182: the unreached-surface fence covers the whole package, and it knows this repository is
  not the world.** Two bugs were what made whole-package scope look "noisy": the resolver
  mis-resolved every relative import inside an `__init__.py` (so all five `cli.*.add_subparser` read
  as dead while `cli/__init__.py` calls them by name), and there was no way to say *the caller is an
  endpoint repo*. `scripts/author_surface_allowlist.txt` says it now, one symbol at a time with
  `repo/path:line` as proof, after pgw#1179 deleted `trt_engine.build` whose caller is a conversion
  endpoint in `training-endpoints` backing the PRICED `produce-trt-engine`. `--siblings` mechanises
  the check across six sibling repos, optionally every remote branch, and the priced-function
  manifest — a symbol behind a priced function has a consumer even when no code imports it. 144
  findings became 113 with no code deleted; 113 are baselined under three dispositions (wire it,
  delete it, or prove the caller is elsewhere).
