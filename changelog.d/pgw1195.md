- **pgw#1195: the ratchet's own writer was corrupting the ratchet.** `--write-baseline` dumped
  `sorted(current)` over the whole file, which both destroys the prose the guard now READS
  (`baseline_rows` parses the OWNER/EXPIRY notes out of it) and duplicates every row a documented
  section already lists — 14 of them, twice: written naively in pgw#1182, deduped by hand in
  pgw#1190, reintroduced in pgw#1192 by regenerating again. **A hand-fix a tool re-breaks is not a
  fix.** The writer now preserves the header byte-for-byte, appends only rows it does not already
  list, and reports a documented row that has stopped being a finding rather than silently editing
  prose. Regeneration is idempotent: byte-identical output, verified.
