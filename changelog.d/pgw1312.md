- **`GEN_WORKER_FORBID_CPU_OFFLOAD` no longer exists, and every CPU-offload activation now
  confesses.** Paul, 2026-08-17: *"There is no FORBID_CPU_OFFLOAD. Envs are only for configs +
  secrets, they are not logic-gates. That is a logic gate. We ALWAYS allow CPU-offload, and
  encourage it — but when it happens we warn LOUDLY so the error can be caught (we don't want
  to serve degraded in production)."* pgw#929's veto arm (`cpu_offload_forbidden`,
  `_refuse_cpu_offload`, `CpuOffloadForbidden`), the `config.loader` declaration, the
  `config_reads_allowlist.txt` tripwire row and its `lint_config_reads` justification are all
  deleted; setting the variable does nothing. The question it answered — "may this host
  offload" — is not a question.

  The question that IS one is "did it happen", and only the OOM-triggered descent was
  answering it. `apply_low_vram_config` is the single chokepoint every activation route
  reaches (a plan-time rung selected against free VRAM, `place_pipeline`'s reactive descent,
  `rearm_offload`'s serve-time fallback), and at `model_offload`, `group_offload` and
  `sequential` it logged `low_vram: <rung> applied` at INFO and emitted nothing. A hub-spawned
  worker has no stdout, so a pod that chose to offload before any OOM said so to no one.
  `models/memory._report_offload_engaged` is now the one home: a `DEGRADED_MODE=engaged`
  line for a human plus one `serve_degrade` activity event with `phase=cpu_offload_engaged`,
  both derived from the same measured numbers so they cannot disagree. Never a gate — the
  placement has already succeeded when it runs.

  `GEN_WORKER_HOST_MOVE_GUARD` is a different, correctness-class guard (silent host tensor
  moves that blow the cgroup RAM budget) and is untouched: still on by default.

  Red-verified against `12316afd` on three distinguishable arms: the model and sequential
  rungs raised `CpuOffloadForbidden` under the env this box exports (which also meant the
  works-always OOM descent DIED rather than degrading on such a host); `group_offload`, which
  the veto never touched, failed on `expected ONE confession, got 0` — the silence, proven
  independently of the env; and disarming the emitter leaves the offload itself working and
  the hub hearing nothing, so the assertion is load-bearing rather than decorative. A resident
  rung stays silent in both trees.
