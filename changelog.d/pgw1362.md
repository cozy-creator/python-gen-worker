- **The test corpus starts moving from incident names to domain modules.** 406 of pgw's 441 test
  modules were named for the issue that produced them, which says *when* a test was written and
  never *what* it exercises. Wave 1 takes the hub-wire cluster: 27 modules / 7,246 lines fold into
  5 domain modules / 5,940 lines — `test_wire_protocol.py`, `test_hub_transport.py`,
  `test_receipts_trust.py`, `test_telemetry.py`, `test_media_transport.py`. **All 324 tests
  survive**, verified by collecting both trees and comparing the count; the incident lineage
  survives as a one-line section banner plus a one-line docstring on each test, and the full
  narrative stays in the tracker where it belongs.
- **The merge is a strictness win, not a strictness trade.** 13 of the 27 folded modules were on
  the `ignore_errors` burn-down list — wholly unchecked by mypy. The 5 merged modules are typed
  clean at the test posture, so 13 names come off the list and none go back on: the
  `lint_mypy_ratchet.py` high-water mark drops 255 → 242.
- **One guard got wider on the way through.** `test_retired_wire_enum_spelling_is_gone` (th#2082)
  excluded its own file from the `git grep` — which after the fold would have blinded it to a
  whole domain module's worth of neighbours. The searched strings are now assembled from pieces so
  the file is not its own hit, and the grep covers the entire tree with **no exclusion at all**.
- **Every merged module was mutation-probed rather than assumed.** One load-bearing guard per
  module was proved able to go RED against a real production mutation (receipt signature check
  disabled; `cuda_version` dropped in `_parent_resources`; the stage-map residual zeroed; a
  retired enum spelling reintroduced; the outbox's evidence requeue emptied). Recorded on
  pgw#1362 with the mutation each used.
