- **A NAMED sibling that is not on disk is a REFUSAL, and the count printed is what was READ.**
  `lint_unreached_surface.py --siblings` still named `training-endpoints`, whose checkout became
  `~/cozy/jobs`; the loop `continue`d past the corpus it could not find while the success line
  printed `len(SIBLINGS)`. So the scan reported *"5 files/branches scanned across 6 siblings, 0
  unlisted consumers"* and exited 0 — green, on five sixths of the corpus. pgw#1323 fixed the NAME
  for the `inference-endpoints` half; this fixes the SHAPE, which is what makes the next rename loud
  instead of silent. `_sibling_texts` now returns `(corpus, scanned, refusals)`; a named sibling
  with no checkout, a `git grep` exiting above 1 (rc 1 is "no match", a real answer), and an
  `ls-remote` failure under `--branches` are all refusals naming the path. The success line reads
  `N/M siblings (names)` off `scanned`, never off the declared list, and the old
  `if not corpus: return 0` silent green is gone — zero readable siblings is six refusals and exit
  1. CI cannot run this mode for real, so a `--siblings-selftest` forces both arms in a temp
  workspace and is now a `fast gates` step.
