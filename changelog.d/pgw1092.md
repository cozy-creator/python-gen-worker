- **pgw#1092 (§4.28, the worker half of th#1751 W4): the FORGE and its wire vocabulary are deleted.**
  Paul, 2026-08-10: *"just delete the entire mint-request + forge system."* tensorhub cut its 14.6k
  lines in `9447ffde` and deliberately left the WIRE inert; this is the coordinated window that
  retires it, pgw-first per th#1562's proto-drift ordering.

  **The wire.** `WorkerResources.worker_mode` (field 12) and `CompileTarget.requested_cell_axes`
  (field 11) become `reserved` — number AND name, per §1.27(f)'s within-major rule — and both Hello
  builders stop filling them. `WORKER_MODE` disappears from `Settings`, from the loader's env map,
  from `docs/environment.md` and from `scripts/probe_sync.sh`. **Wheels ≤ 0.112.0 still SEND these
  fields and are unaffected**: proto3 skips an unknown field at decode, which
  `test_wire_facts_pgw876` now proves by hand-encoding field 12 onto a serialized `WorkerResources`
  and asserting a full-length parse. `requested_cell_key` and `CellLookup` SURVIVE — they are
  §4.27's lookup half, and only the axes twin was ever "forge mint parameters".

  **The pod class.** `mint_goal.py` (the 230-line scheduled-mint driver) is deleted whole with its
  `forge_terminal` activity kind and four typed terminals; `worker_goals` keeps one goal, `serve`,
  and loses `mint` / `drives_mint` / `retires_when_mint_completes` / `tenant_reserve_applies` /
  `wire_declaration` / `MINT_ONLY` / `from_settings` and the `declared` bootstrap-spelling pair.
  `lifecycle` no longer spawns a mint-goal driver, `mint_child._install_goals` is gone, and
  `Executor.background_mint_tasks` / `fleet_cells.published_cells` / `refused_publishes` go with the
  only caller that ever read them — every write they recorded is already a typed activity event
  (`self_mint_publish` / `self_mint_publish_failed` with its `phase=`) the hub receives.

  **The reserves TIGHTEN, and that is the point.** The forge's contribution to the compile pool was
  three *relaxations*: `FORGE_RSS_RESERVE_BYTES` (1 GiB instead of 4), a zeroed `SERVING_HEADROOM_CPUS`
  and a zeroed `DEVICE_RESERVE_BYTES`, plus `mint_budget`'s zeroed tenant activation and pgw#1053's
  park-the-eager-pipeline path. All are deleted: the only mint left is the one a SERVING pod runs in
  the background on a cell miss (pgw#784), so there is always a tenant and every reserve is
  unconditional. **Every pgw#992 clamp is untouched and proven so** — the simultaneity bound still
  holds K at 2 on the incident card against the banked 6.02 GiB per-entry peak, now against a budget
  one `DEVICE_RESERVE_BYTES` smaller.

  `test_worker_goals_pgw930.py` is replaced by `test_forge_vocabulary_deleted_pgw1092.py`, which
  asserts each deletion by the state it was RED in, proves a pod carrying a stale `WORKER_MODE=forge`
  serves normally and that the stale value does not move the `env_seal` digest, and sweeps the tree
  for live `forge` references.
