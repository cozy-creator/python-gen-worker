- **The ship-code-as-is serving layer lands (`gen_worker.serving`), eager-first.**
  Authors ship code as-is: `endpoint.toml`'s `main =` module carries one endpoint
  class inheriting the REQUIRED minimal base (`gen_worker.Endpoint` — typed
  `setup(ctx)`/`teardown(ctx)` hooks only; framework capabilities arrive via ctx
  ONLY, by ruling); the worker loads it and routes requests to handlers by
  function name — no catalog, no ModelSpec, no codegen. `ServeContext` is the
  `main_v2.py` surface (`ctx.checkpoint_dir` — the lane-contract-converted
  checkpoint tree, `ctx.lane`, `ctx.checkpoint_defaults(T)`, `ctx.adapter`,
  `ctx.is_trace`, `ctx.step_callback`) on the existing RequestContext.
  `python -m gen_worker.serving` boots an endpoint locally with zero hub.
- **Adopt-first boot rides `ctx.compile` (imperative marking, the torch.compile
  idiom):** `self.pipe.unet = ctx.compile(self.pipe.unet)` in the author's own
  setup is where compiled graphs swap in — the torchcg `AdoptSession` forms
  BEFORE author code runs (exact-env audit: lockfile-closure hash vs release
  stamp, loud typed refusal), store hits arm behind a per-module dispatcher,
  misses come back eager with an ordered hole list — the pgw#1371 mint
  handoff, armed per graph as each publish lands, no reboot. Hub store is thin
  over th#2133's route (transport seam explicit); partial-hit everywhere.
  `boot_stages` gains the `adopt_pull` span (`graphs_from=release`,
  per-outcome counts) — the new flow's replacement for the keyset/derive span.
- Vendored torchcg takes upstream's tcg#41/tcg#45 wave (lanes, two-level
  identity, graph-set document, requirements manifest, discovery, hollow
  session, `GraphStore`, `AdoptSession`).
