- **pgw#1134: a mint blocker whose exit criterion is a MEASUREMENT can now gather it — `python -m gen_worker.measure_child`.** ltx-video-2.3's OQ-3 (*"whole-graph export OOM, never re-measured on the served w8a8 lane"*, ie#651) was unanswerable through either front door: `mint_child._assert_family_mintable` refuses while ANY blocker is open — so the run that would CLOSE OQ-3 was refused BY OQ-3 — and the ungated `boot_trace_child` composes structure-only, which `structure_only._refuse_artifact_lanes` refuses BY NAME on a w8a8 / w4a4 / svdq artifact tree. Resolving the blocker to unblock its own run is the circularity pgw#1115 exists to prevent, so the third door is a distinct entry: an operator points it at the `*.mint.json` they would have minted, it runs the mint's own load and `aot_mint.trace_for_key` (now with the INDUCTOR half, `compile_now=` — *"an export-only trace never exercises the whole-graph planner this blocker names"*), and it writes `export_peak_device_bytes` / `export_peak_device_reserved_bytes` per entry in the mint's own vocabulary.

  **It cannot publish, structurally rather than by convention.** `MeasureJob` is a different wire struct that declares NONE of `MintRequest`'s output-side fields (`target`, `work_root`, `report`, `resume`, `arm_token`, `phases_snapshot`), so msgspec drops the artifact destination at the boundary and the process never holds a path to write one to; `MeasureReport` carries no artifact, digest or cell key; the inductor output of each row is counted and DELETED before the report is written. Four fences hold it: the wire subset, the report type, an AST scan of the module against the publish surface, and an end-to-end run with every publish seam (`fleet_cells.publish_self_mint`, `local_cell_store.store`, `aot_serve.artifact_metadata`, `aot_mint.mint_targets`, `mint_child.mint`, `mint_process.run_mint`, `cell_resolve.materialize`, …) wired to raise.

  **The real-weight fallback is scoped to the measurement and nowhere else.** pgw#1080's invariants are untouched: a mint still fails closed on `StructureNotHonored`, and the BOOT trace still refuses a stranded family rather than downloading its checkpoint to state an identity (§4.27 step 1) — a test reads that out of the source. This child accepts real weights because the graph a quantized pod serves is made of those bytes, and it REPORTS which lane it measured (`weights=virtual|real` off the composed pipeline, plus the typed refusal that sent it there), so a weightless claim can never be implied by a run that was not. Measuring resolves nothing: the blocker stays open, the mint keeps refusing, and closing it stays a reviewable `resolved=True` that cites the number.

  New `activity.KIND_MEASURE_ONLY` — deliberately not `aot_mint_phases`, because a measurement counted as a mint would be the first row of a lie — with its own exhaustive refusal vocabulary, fenced by the pgw#1116 scan pattern. `tests/test_measure_only_pgw1134.py` drives all of it on a REAL w8a8 tree written by the SDK's own `quantize_tree_w8a8`, through the real loader and the real `_export_entry`; the whole file is uncollectable on the pre-fix tree, which is the RED. Cardless: the AOTI compile is faked at the one seam `_export_entry` compiles through, and the ltx measurement itself is pod work recorded as a checklist item on OQ-3.
