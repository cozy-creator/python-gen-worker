- **Four docs promised a refusal that does not exist anywhere in this repo.** `docs/compile-cache.md`
  and `docs/endpoint-authoring.md` stated that `aot_package.folded_weights` "refuses, per entry, any
  lifted weight the compiled artifact does not declare" behind a `constant_folding_fenced` axis —
  pgw#1270 deleted `aot_package`, and both names survived only in that prose. The same file sourced
  the lane-election story to a deleted `kernel_path.py`. The bullets now say what is actually true
  (the `use_runtime_constant_folding=True` deferral is real; the proof arm is not) rather than being
  re-pointed at a plausible symbol. A doc that converts an absent guard into a believed one is worse
  than one that merely misdescribes live code.
- `benchmarks/swap_latency.py` shipped an unconditional `ImportError` — it imported thirteen names
  from a module pgw#883 deleted, so it could not be imported at all while sitting inside a root the
  pickle-reader fence scans. Deleted. The `benchmarks/` entry stays in that linter's default roots on
  purpose: a missing directory yields nothing, and removing the entry would leave a re-added
  `benchmarks/` silently unscanned.
- The pickle ban itself re-verified and holding: zero `torch.load`, zero `pickle.load*`, zero
  `import pickle` in `src/`, the `materialize_pickle_to_safetensors` converter gone, and
  `lint_pickle_readers` passing with its self-test red.
