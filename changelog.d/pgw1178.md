- **pgw#1178 (th#1834 P0-D): the dead JIT producer is DELETED — 969 lines — and deleting it proved the adopt lane is a consumer with no producer.** `local_cells.py` (311, zero production importers since pgw#1127 re-pointed `cli/run.py` at `local_serve.enable_compiled`) and its 519-line test go together with the recipe they were the only caller of: `compile_cache.mint_artifact`, `_compile_and_warm`, and the two warm helpers those stranded (`_warm_call`, `_warm_text_lens`, test-only afterwards). `scripts/lint_unreached_surface.py`'s `EXEMPT_TARGETS` row for `gen_worker.local_cells.enable_compiled` goes with the module it named — its own text said *"this row exists for exactly as long as `local_cells.py` does"*, and a row whose target no longer exists is RED.
- **`mint_artifact` was the LAST producer of a `torch-inductor-cache` artifact anywhere in this tree.** Removing it surfaced `compile_cache.pack`, `compile_cache.artifact_metadata` and `guard_closure.closure_manifest` as production-unreached — which answers the question `scripts/unreached_surface_baseline.txt` has held open since 2026-08-08 (*"whether anything still PRODUCES a delivered torch-inductor-cache artifact for that path to consume"*): **nothing does**, and the live adopt lane `enable → stage_artifact → unpack/verify → _activate_staged` is consuming a format with no writer. The three are baselined with an owner (the per-entry atom) and an expiry rather than deleted: `pack`/`artifact_metadata` are the construction vehicle for ~40 test call sites that drive the still-live transaction with REAL tarballs, `artifact_metadata`'s output format is a cross-repo contract cozy-local's `internal/cli/cells.go` parses, and `closure_manifest` belongs to the KEPT guard-closure doctrine.
- **The P0-D inventory was wrong and was caught before anything was cut: 22 of its 25 `compile_cache` targets are ALIVE.** `compile_cache.enable` is live production (`models/provision.py:780`) and keeps `stage_artifact`, `_activate_staged`, `_merge_staged_cache`, `unpack`, `verify`, `artifact_drift`, `local_cell_mismatch`, `composition_fingerprint` and `capture_env` (reached only through the module-level alias `seed_env`) reachable; `apply` — a MUST-KEEP — keeps `_install_fx_system_shim`, `_normalize_system_info`, `_set_semantic_cache_tag` and `_semantic_cache_tag`; `executor.py` calls `fx_cache_failure_report` twice, anchoring `fx_key_forensics`, `_fx_entry_lines` and `_fx_components`. **`_clean_tarinfo` is a shared tar-hardening primitive `aot_serve.py` and `trt_engine.py` both import** — deleting it would have failed the AOT and TRT lanes at import time. `seed_artifact` is correctly caller-less and is deliberately NOT deleted here: it is the vehicle for adopt-lane coverage in four test files, one of which pins an `AdoptError` kind only it produces, and it sits on the surface the durability rework rewrites.
