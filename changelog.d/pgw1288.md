- **The last four compiled-graph constants the worker re-declared are deleted; TCG is the one
  authority.** `KEY_AXES` / `TCG_GRAPH_CLASS_BLOCK` (`graph_facts.py`), `KIND`
  (`local_cell_store.py`) and `ARTIFACT_KIND` (`aot_serve.py`) are gone with no re-export aliases —
  their readers import `REQUIRED_AXES`, `GRAPH_CLASS_BLOCK` and `ARTIFACT_KIND` from the compiled-graph
  library directly. The two unfenced copies are the point: nothing compared them to their upstream,
  so a rename there would have gone unnoticed — one field over from the entry/graph_class outage and
  still live. Floor raised to `>=0.6,<0.7`; without it the deletion breaks the worker at import.
  `publish_intent`'s axis loop is now parametrised over the upstream export rather than restating
  three axis names, closing a read site where shortening the list to two left the whole suite green
  and would have let an entry with no toolchain axis reach the hub.
