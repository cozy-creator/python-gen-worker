- pgw#1569: `scripts/` pruned 83 → 15 files and `tests/` 230 → 200 modules. Every incident
  lint guard (and its allowlist/baseline/census), dead bench/probe/parity script, `svdq_bench/`,
  `mint_rig/`, the `*-drift.sh` layer, source-grep/absence/mock-the-subject tests and dead-mechanism
  tests are deleted; `fast gates` drops 47 → 17 steps. Kept: release/vendor infra, the five
  shared-corpus digest gates, and the pickle/unbounded-read security fences.
