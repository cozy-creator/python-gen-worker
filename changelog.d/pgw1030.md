- **pgw#1030: mint-lane dead code deleted — ~430 lines proven dead by consumer
  census, none of it participating in a digest, an envelope field, or a wire
  struct.** Deleted: `aot_export_parallel` (184 lines whose `decide()` recorded
  the width a PARALLEL export would have run at — export is unconditionally
  serial and nothing ever consumed the decision; the module also duplicated the
  compile pool's card-budget arithmetic, the drift class); the
  `graph_hash.combined_graph_hash` duplicate (`aot_serve.combined_graph_hash`
  is the live formula the stamped identity folds); `ExportSpec.batch` (zero
  readers — the traced batch is a declared GraphClass coordinate since
  pgw#739); the `cell_key.compute(closure_roots=)` parameter (pgw#990 removed
  code identity from the key and the body never read it; no caller passed it);
  `_export_entry(rows=)` (accepted, never referenced);
  `EntryCompilePool(resume_dir=)` (production wires the resume bank through
  `aot_resume.set_root`; tests now use the production mechanism); the
  zero-caller one-line wrappers `fleet_cells.delegatable`,
  `mint_delegate.delegated` and `aot_resume.resume_enabled` — all invisible to
  `lint_unreached_surface`'s documented wrapper-exemption blind spot;
  `aot_declaration.named_dynamic_rows` (zero callers); and the
  `GEN_WORKER_AOT_ENTRY_CHILD` child-env marker, written for four months and
  read by nothing. `aot_compile_spans.dark_fraction` was audited and KEPT — the
  pgw#830 attribution test uses it as a live quality gate over real mint
  tables, not as self-coverage.
