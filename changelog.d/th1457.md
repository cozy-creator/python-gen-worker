- **pgw#891/th#1457: the `ExecutionSpec` contract is vendored — this repo now compiles against the
  immutable execution identity.** `proto/worker_scheduler.proto` resyncs from tensorhub's canonical
  copy at digest `19f8367c…`, adding `RunAttempt`, `AttemptId`, `ExecutionSpec`, `Arm`,
  `ArtifactIdentity`, `ConfigSnapshot`, `InputAssetRef`, `DeliveryGrant` and `PresignedAsset`, with
  `src/gen_worker/pb/` regenerated.
- **SCHEMA ONLY — nothing produces or consumes it yet.** `RunJob` remains the live dispatch until
  the coordinated cutover (pgw#891 PRs 3-5 + the paired deploy). There is **no dual-read, no
  translator and no negotiation**: the old message is not being kept, it is being retired on a
  schedule that is written into the proto so the additive state cannot quietly become permanent.
- **The split that shapes the message.** `RunJob` mixes identity (`Snapshot.digest`,
  `SnapshotFile.digest`, `ChunkRef.sha256`) with delivery (presigned URLs, `capability_token`) in
  one message, so any digest over it moves when a credential rotates — which makes pgw#891's
  acceptance *"grant rotation does not change execution identity"* unsatisfiable by construction.
  Every digest now lives in `ExecutionSpec`, every URL and token in `DeliveryGrant`, joined **by
  content digest** so neither owns the other.
- **This side lands FIRST, by design.** `scripts/vendor-proto.sh` says so and tensorhub's drift
  gate is the reason: its layer 2 reads this repo, so th#1457's PR #915 is deliberately red until
  this merges. The interlock makes a wrong merge order fail loudly rather than silently forking the
  contract — trust it rather than working around it.
- Vendored from a **pinned commit** (`00d5559a`), not from the sibling lane's live worktree, so what
  landed here is exactly what they pushed. Upstream self-consistency is verified by the script
  before the copy, and layer 1 re-verified after.
