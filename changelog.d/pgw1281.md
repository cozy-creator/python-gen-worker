- The pickle ban gets a fence CI actually runs, and the two model entry lanes it never covered
  are closed (pgw#1281, HARDCUT E5/M2/C3). The standing scans lived in `tests` — a job that
  gates nothing since pgw#1264 — and read `src/` only, for `torch.load(` plus pgw#1275's three
  `pickle` shapes. `scripts/lint_pickle_readers.py` runs in `fast gates` over `src/`, `tests/`,
  `tests_v2/`, `scripts/`, `examples/` and `benchmarks/`, and adds `joblib`, `dill`,
  `cloudpickle`, `allow_pickle=True` and `read_pickle`. **No path allowlist**: a site is
  permitted only where the line carries a proof marker AND its file raises and asserts a pickle
  refusal, so an exemption cannot hide behind a filename. `--selftest` plants a call in each
  tree and proves the fence goes red, including on a marker with no proof behind it. It found
  two real holes. **ModelScope was never guarded** — it mirrors HF-shaped repos verbatim, `.bin`
  components included, and both call sites (`download.ensure_local`, `provision.resolve_local_path`)
  handed `snapshot_download`'s return straight to a loader; one `download_modelscope` now owns
  the lane and refuses on what landed, typed and terminal, before any reader sees it. **Civitai**
  was safe by construction (its selector allow-lists `.safetensors`/`.gguf`) but blamed the
  registry — a pickle-only version said `civitai_no_supported_files`; it now says why. The rig's
  own checkpoint was a `.bin` read back through `torch.load` — the exact shape the production
  lane refuses — and is safetensors. Also: `Resources(min_disk_gb=)` (pgw#732) is DELETED, never
  declared by any endpoint in any repo since it shipped, and `compile_cache.IDENTITY_AXES` is
  gone — a tuple naming a `verify()` deleted two waves earlier and claiming a parity with
  `aot_serve.IDENTITY_AXES` that was never true (5 axes against 3). The checkable parity replaces
  it: every axis an exported `.pt2` is pinned to must be one the single runtime probe can state.
