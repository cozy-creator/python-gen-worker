- **pgw#1141 REOPENED (the second half): a boot-adopted cell is on the EXPORTED lane, and the setup pass now reads that off the OBJECT.** 0.111.0 shipped §4.31's warmup-barrier deletion and a real pod proved it works — and served eager anyway, first try (POD PROOF #4, RTX A4500 sm_86): `boot_adopt=hit` (10.3 s) → `cell_numerics=armed_undispatched` ("It STAYS ARMED and serves") → 677 ms later `serve_eager_posture target_applicability_incomplete functions=()` → `armed_target_unresolved armed=False targets_resolve=True` → `serve_degrade` → eager for life. PR #658's claim that this chain was *"structurally unreachable"* on the exported lane was false on the shipped wheel: the disarm had moved one branch over, not disappeared.

  **The locus is one unfed registry.** `aot_serve.is_aot_ref` answers out of `_KNOWN_AOT_KEYS`, a process-global set fed by `note_aot_key`, and pgw#1033 wrote the rule as a CONVENTION — *"whoever reads a `cell_key` off an aot-inductor envelope registers it"*. It had exactly two feeders, `fleet_cells.arm_from_local_store` and `adopt_delegated_mint`, **both self-produced routes**. The ORDERED arm — `fleet_cells.arm_ordered`, which is every hub Plan and every §4.27 boot-adopt — fed it nothing. So an adopted cell wrapped itself onto a live pipeline while its own ref still answered "not an AOT cell", and every reader asking *"is this the exported lane?"* sorted it onto the DYNAMO lane, whose per-class **cache-hit ledger an AOTI artifact can never move**: `proves_inductor` went True, `aot_proof_before` came out EMPTY, §4.31's keep-the-arm branch could not fire for the one object it exists for, the cell scored calls=0, was folded into `unproven`, got `function_proofs[id]=set()` and was unwrapped — and `exported_arm` was then False at the install too, so `permitted_names` came from that empty proof set. `functions=()` made the rest inevitable. The dynamo ledger keeping its teeth is CORRECT (§4.31: a dynamo arm that misses RECOMPILES — correct output, silently slower, no exception for try-serve to catch); the defect is that an exported cell was being handed to it.

  **Fixed in two places, because one was a convention and the other is a structure.** (1) `aot_serve.load_and_wrap` registers the key **at the wrap** — the single seam every arm route passes and the moment the fact becomes true — so a new arm route inherits the registration instead of having to remember it. (2) The executor's three lane readers (`_proves_by_fx`, `aot_proof_before`, `_install_compile_targets`'s `exported_arm`, plus §4.31's own keep branch and the `dynamo_unexercised` fold) now go through one predicate, `executor._exported_arm`, which asks the OBJECT via the new `aot_serve.holds_exported_cell` — deliberately distinct from `is_armed`, so a REVOKED cell still reads as exported (it must never be advertised) while an object carrying no cell reads as an ordinary dynamo/eager one. The disarm authority can no longer be exercised over an object carrying a live cell even if no registry ever learned its key.

  **Every honest de-arm keeps its teeth**, and each is asserted: a revoked artifact installs no target and names `compiled_degraded`; the operator's `eager_only` order (§4.32 item 4) still suppresses the arm outright; measured evidence against a dynamo object still disarms it; and the pgw#868 mint-time parity gate is untouched.

  **The RED runs the pod's chain off-pod, cardless.** `tests/test_adopted_arm_lane_pgw1141b.py` drives the real `ensure_setup` → `_injection_kwargs` → `_enable_compiled` with a real `_ArmOrder(adopt=…)` → real `fleet_cells.arm_ordered` → the real receipt gate against a real RSA-signed receipt from a real HTTP hub → `provision.arm_aot` → `load_and_wrap` on a real packed cell whose `ck1` key is genuinely restatable from its own recorded facts → the real warmup, the real proof pass and the real `_install_compile_targets` / `_assert_armed_targets_installed`. On `origin/master` it reproduces the pod verbatim — `compile target omitted for generate: immutable object applicability is incomplete (functions=() … owned_slots=['pipeline'])` and `1 ARMED compile object(s) own NO installed target (armed=False targets_resolve=True)`. Three seams, all WEST of the locus and all named: `_boot_adopt` (its derive+resolve half is driven for real by pgw#1116), `provision.load_slot` (the weights read), and pgw#868's `_load_package` (the AOTI `.so`, the one piece deferred to a pod). **Why the previous tests could not see it: their fleet-policy stand-ins call `aot_serve.note_aot_key(key)` by hand** — the one thing no production arm route did.

  Also: `examples/micro-diffusion/README.md`'s pod runbook §4-§7 was unexecutable and burned pod-leg time. `POST /v1/admin/compile-cells` no longer exists (§4.28 retired the mint-only pod class — a serving pod self-mints in the background), so the runbook now buys a serving worker and drives one request; `size` is an ENUM (`"256x256"`), not a `{"width":…,"height":…}` object, which is a `400`; and §7 names the four typed rows that decide GREEN.
