- **pgw#1232 / pgw#1235: model and compiled-artifact transfer now use HashRepo.**
  The worker composes Tensorhub authorization and producer-recovery policy over
  HashRepo's v1 manifests, verified transfers, local object store, logical refs,
  materialization and garbage collection. The duplicate chunk downloader,
  uploader, CAS, journal and legacy split-file snapshot paths have been removed;
  publishing, model snapshots and AOT delivery now share one worker CAS root.
- Hard-cut the compiled-graph worker protocol and runtime together: one importable
  machine-readable contract authority, compiled-graph routes and receipts only,
  and no legacy cell aliases or duplicate receipt/revocation fetch paths.
- Consume `torch-compiled-graphs` 0.3.x as the sole compiled-graph core and ship
  the producer contract corpora as package data for version-pinned consumers.
