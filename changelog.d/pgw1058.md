- **pgw#1058: the LAST A1 gate — a published cell that admits none of its
  own labelled classes is now impossible to publish, and the silent dtype
  guess that minted one is unconstructable.** Attempt 30's first-ever
  published aot-inductor cell (`ck1-9ae7bbea…`) refused all 18 adopt-side
  warmups. Pulled apart off-pod at $0, the filed lying-label hypothesis is
  FALSIFIED from the artifact's own bytes: every entry's label and packed
  manifest faithfully describe the program — the program itself was
  specialized on `timestep: bfloat16[]` because sdxl's declaration omitted
  the row's dtype and `declared_inputs` silently defaulted it to the
  module's weight dtype, while the real scheduler presents `float32[]` at
  the unet boundary (the reported "36/36 static_dim_mismatch" was
  `select()`'s `reasons[:6]` truncation over the 34 wrong-shape entries; the
  shape-matching entry's true refusal is `dtype_mismatch` on timestep).
  Two closures: (1) `Input.dtype` is REQUIRED — a concrete torch dtype or
  the explicit word `"model"` — refused with a named `DeclarationError` at
  construction, so an omitted dtype fails the declaring repo's import, not a
  rented mint pod (endpoints must sweep their declarations on this bump;
  sdxl's fix is ie#627). (2) The declared manifest rows of every entry are
  proven against the artifact's OWN generated `check_input_<i>` guards —
  dtype exact, static dims exact, symbolic dims un-specialized — through ONE
  derivation (`aot_package.admission_drift`) at package time (`MintRefused:
  admission drift`, so a divergent cell never publishes) and at arm time
  (`AdoptError: admission_drift`, so a corrupted one never serves). The
  published cell's defective entry rides the repo as a fixture
  (`tests/fixtures/pgw1058/`): its guards parse, its label proves honest,
  the real warmup class reproduces the refusal, and the corrected
  declaration's manifest is caught against the real bytes by name.
