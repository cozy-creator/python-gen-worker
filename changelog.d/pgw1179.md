- **pgw#1179 (§4.34): the test diet's first pass — six rows that had never run anywhere, and the
  TRT engine's dead producer.** Every deletion is severance-proven rather than judged: the
  pgw#605 heartbeat row sat under a hard `@skip` around `raise AssertionError("unreachable")`;
  `test_boot_phases_arm_pgw764.py` and two publish-observability rows were staged on a "sibling
  lane's uncommitted WIP" that never landed (and pgw#764's subject, `aot_serve.load_and_wrap`, is
  being deleted outright by pgw#1176); the topology byte-identity row probed
  `~/cozy/tensorhub-chaos`, a path the workspace policy calls a read-only leftover, so the
  cross-repo fixture identity it claims to gate is **unverified by anything**; and the divergence
  ledger parametrized over a list the fixture has recorded as empty since 2026-08-02.
  `trt_engine.build` (ONNX export + engine build) and `find_artifact` had no call site on the pod,
  in any script, or in any test — the same shape pgw#1178 found on the adopt lane, so the TRT lane
  now says in its own docstring that it is a consumer with no producer in this repository.
