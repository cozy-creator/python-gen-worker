- **pgw#1157: the mint child's evidence counter died at the first phase change, so a 62-minute compile reached the hub as counterless heartbeats.** RunPod A40 `bgmdxhazxsugmk`, release `6ee9b4d4df2697a53da6f43a`, gen-worker **0.112.0** — pgw#824's instrumentation entirely present and shipped — sat in `trace_graph` for 62 minutes and sent not one counter-carrying beat. The mint was fine: it packed 16 of sdxl's 36 declared entries (`export_s` 1378.52, `compile_s` 2065.36, 76-93 s export and 108-140 s compile per entry, pool held to `entry_workers=3` by vram with `underwidth=5`) and was on course for ~2.2 h before the lane terminated the pod. Nobody could tell that from a wedge, and the lane correctly said so.

  **One captured object.** `mint_delegate._on_evidence` acquired the `mint_child_evidence` counter ONCE and closed over it, while `Activity.counter()` binds a counter to the phase that registered it and `Activity.phase()` FINISHES — unregisters from the process registry — every counter the new phase does not own (pgw#962). The mint crosses `load` → `warmup_forward` → `trace_graph`, so from the first phase change on, every `set_done` fed an object no reader could reach: `activity.on_beat` found no counter in the activity's scope and returned *without emitting*, `progress.self_diagnosis` had nothing to be stale about, and the hub's rule reads a counterless beat as progress by definition (an old worker must never be condemned for a signal it cannot send). The callback now RE-ACQUIRES per tick, which is exactly the contract `Activity.counter()` documents for producers — `progress.counter` is register-or-get, so it costs a dict lookup and re-registers under whatever phase is current.

  Also: the counter is now `compile:mint_child_evidence`. It had no family prefix at all, so `window_for` fell through to `DEFAULT_STALL_WINDOW_S` (300 s) — the mint's patience was the generic default by accident rather than the 600 s the `compile` family declares for exactly this work, and an AOTI entry that spends minutes inside one inductor call is the case that window exists for.

  RED-proven by restoring the captured-counter shape verbatim: 3 of the 5 tests fail, including the one asserting a frozen mint can confess at all. The tests drive the real `Activity`, the real process counter registry, the real `_on_evidence` and the real `activity.on_beat` — no doubles on the path under test.

  The hub half is **th#1822** (in-phase position made durable, plus an alarm for background work on a serving pod, which the health evaluator was skipping entirely).
